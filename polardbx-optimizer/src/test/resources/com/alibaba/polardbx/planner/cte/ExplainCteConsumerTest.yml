SQL:
  - sql: |
      SELECT * FROM t0 WHERE EXISTS (
        WITH cte_sub AS (SELECT v4, v5 FROM t1 WHERE t1.v4 = t0.v1)
        SELECT 1 FROM cte_sub WHERE v5 > t0.v2
      );
    plan: |
      logicalcteanchor(cte_id=1)
        logicalcteproducer(cte_id=1)
          logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        logicalsemijoin(condition="v1 = v1 and v2 = v2", type="semi")
          logicalcteconsumer(cte_id="1_2")
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
          logicalcteanchor(cte_id=0)
            logicalcteproducer(cte_id=0)
              project(v5="v5", v1="v1", v2="v2")
                logicaljoin(condition="v1 = v4 and v5 > v2", type="inner")
                  logicalcteconsumer(cte_id="1_1", projects=[$0, $1])
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0`")
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5` from `t1` as `t1`")
            logicalcteconsumer(cte_id="0_0", projects=[?0, $1, $2], conditions=[>($0, $2)])
              project(1="?0", v1="v1", v2="v2")
                logicaljoin(condition="v1 = v4 and v5 > v2", type="inner")
                  logicalcteconsumer(cte_id="1_0", projects=[$0, $1])
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0`")
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5` from `t1` as `t1`")
  - sql: |
      WITH cte_base AS (
        SELECT t0.v1, t0.v2, t1.v5, t1.v6 FROM t0 JOIN t1 ON t0.v1 = t1.v4
      ),
      cte_win AS (
        SELECT v1, v2, v5,
               RANK() OVER (PARTITION BY v1 ORDER BY v5) AS rnk,
               SUM(v5) OVER (PARTITION BY v1) AS win_sum
        FROM cte_base
      )
      SELECT * FROM t0
      WHERE t0.v1 IN (
        SELECT v1 FROM cte_win
        WHERE rnk = 1 AND win_sum > t0.v2
           OR v2 = t0.v3
      );
    plan: |
      logicalcteanchor(cte_id=0)
        logicalcteproducer(cte_id=0)
          logicalview(tables="t0[p1,p2,p3],t1[p1,p2,p3]", shardcount=3, sql="select `t0`.`v1`, `t0`.`v2`, `t1`.`v5` from `t0` as `t0` inner join `t1` as `t1` on (`t0`.`v1` = `t1`.`v4`)")
        logicalcteanchor(cte_id=1)
          logicalcteproducer(cte_id=1)
            project(v1="v1", v2="v2", f4w0$o0="f4w0$o0", f5w1$o0="f5w1$o0")
              filter(condition="f4w0$o0 = ?0 and f4w0$o0 = ?0")
                window(v1="v1", v2="v2", v5="v5", f4w0$o0="f4w0$o0", f5w1$o0="window#0sum($2)", reference windows="window#0=window(partition {0} order by [] range between unbounded preceding and unbounded following aggs [sum($2)])")
                  window(v1="v1", v2="v2", v5="v5", f4w0$o0="window#0rank($2)", reference windows="window#0=window(partition {0} order by [2 asc-nulls-first] rows between unbounded preceding and current row aggs [rank($2)])")
                    logicalcteconsumer(cte_id="0_0")
                      logicalview(tables="t0[p1,p2,p3],t1[p1,p2,p3]", shardcount=3, sql="select `t0`.`v1`, `t0`.`v2`, `t1`.`v5` from `t0` as `t0` inner join `t1` as `t1` on (`t0`.`v1` = `t1`.`v4`)")
          logicalcteanchor(cte_id=2)
            logicalcteproducer(cte_id=2)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            logicalsemijoin(condition="v1 = v11 and v2 = v2 and v3 = v3", type="semi")
              logicalcteconsumer(cte_id="2_1")
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
              project(v1="v1", v11="v10", v2="v20", v3="v3")
                logicaljoin(condition="v1 = v1 and ((= and win_sum > v2) or v2 = v3)", type="inner")
                  logicalcteconsumer(cte_id="1_0", projects=[$0, $1, $3, =($2, ?0)], conditions=[=($2, ?0), =($2, ?0)])
                    project(v1="v1", v2="v2", win_sum="f5w1$o0", =="f4w0$o0 = ?0")
                      filter(condition="f4w0$o0 = ?0")
                        window(v1="v1", v2="v2", v5="v5", f4w0$o0="f4w0$o0", f5w1$o0="window#0sum($2)", reference windows="window#0=window(partition {0} order by [] range between unbounded preceding and unbounded following aggs [sum($2)])")
                          window(v1="v1", v2="v2", v5="v5", f4w0$o0="window#0rank($2)", reference windows="window#0=window(partition {0} order by [2 asc-nulls-first] rows between unbounded preceding and current row aggs [rank($2)])")
                            logicalcteconsumer(cte_id="0_1")
                              logicalview(tables="t0[p1,p2,p3],t1[p1,p2,p3]", shardcount=3, sql="select `t0`.`v1`, `t0`.`v2`, `t1`.`v5` from `t0` as `t0` inner join `t1` as `t1` on (`t0`.`v1` = `t1`.`v4`)")
                  logicalcteconsumer(cte_id="2_0", projects=[$0, $1, $2])
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
DDL:
  t0:
    CREATE TABLE `t0` (
    `v1` int(11) NOT NULL,
    `v2` int(11) NOT NULL,
    `v3` int(11) NOT NULL,
    PRIMARY KEY (`v1`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8 partition by hash(`v1`) PARTITIONS 3;

  t1:
    CREATE TABLE `t1` (
    `v4` int(11) NOT NULL,
    `v5` int(11) NOT NULL,
    `v6` int(11) NOT NULL,
    PRIMARY KEY (`v4`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8 partition by hash(`v4`) PARTITIONS 3;

STATISTICS:
  t0:
    168648768
  t1:
    6860984320
CONFIG:
  CTE_MODE:
    REUSE
  FORCE_HOLISTIC_SUBQUERY_UNNEST:
    true
  EXPLAIN_CTE_CONSUMER:
    true
  defaltxxAPPName.isNew:
    true
