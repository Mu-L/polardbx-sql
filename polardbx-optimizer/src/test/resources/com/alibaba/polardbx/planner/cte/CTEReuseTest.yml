SQL:
  - sql: |
      with x0 as (select * from t0), x1 as (select * from x0) select * from x1 order by v1 limit 10;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            cteconsumer(cte_id="0_0")
          topn(sort="v1 asc", offset=0, fetch=?0)
            cteconsumer(cte_id="1_0")
  - sql: |
      with x0 as (select * from t0), x1 as (select * from t1) select * from (select * from x0 union all select * from x1 union all select * from x0) tt;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          unionall(concurrent=true)
            cteconsumer(cte_id="0_0")
            cteconsumer(cte_id="1_0")
            cteconsumer(cte_id="0_1")
  - sql: |
      with x0 as (select * from t0), x1 as (select * from x0) select * from (select * from x0 union all select * from x1 union all select * from x0) tt;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            cteconsumer(cte_id="0_0")
          unionall(concurrent=true)
            cteconsumer(cte_id="0_1")
            cteconsumer(cte_id="1_0")
            cteconsumer(cte_id="0_3")
  - sql: |
      with x0 as (select * from t0) select * from (   with x1 as (select * from t1)    select * from x1 join x0 on x1.v4 = x0.v1) tt
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          hashjoin(condition="v1 = v4", type="inner")
            cteconsumer(cte_id="1_0")
            cteconsumer(cte_id="0_0")
  - sql: |
      with x0 as (select * from t0) select * from x0 x,t1 y where v1 in (select v2 from x0 z where z.v1 = x.v1)
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
          nljoin(condition="true", type="inner")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
            semihashjoin(condition="v1 = v2 and v1 = v1", type="semi", build="inner")
              cteconsumer(cte_id="0_0")
              cteconsumer(cte_id="0_1", projects=[$1, $0])
  - sql: |
      with x0 as (select * from t0) select * from x0 t,t1 where v1 in (select v2 from x0 where t.v1 = v1)
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
          nljoin(condition="true", type="inner")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
            semihashjoin(condition="v1 = v2 and v1 = v1", type="semi", build="inner")
              cteconsumer(cte_id="0_0")
              cteconsumer(cte_id="0_1", projects=[$1, $0])
  - sql: |
      with xx as (select * from t0) select * from xx as x0 join xx as x1 on x0.v1 = x1.v1;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        hashjoin(condition="v1 = v1", type="inner")
          cteconsumer(cte_id="0_0")
          cteconsumer(cte_id="0_1")
  - sql: |
      with x0 as (select * from t0), x1 as (select * from x0) select * from x1;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            cteconsumer(cte_id="0_0")
          cteconsumer(cte_id="1_0")
  - sql: |
      with x0 as (select * from t0), x1 as (select * from x0), x2 as (select * from x1), x3 as (select * from x2) select * from x3;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            cteconsumer(cte_id="0_0")
          cteanchor(cte_id=2)
            cteproducer(cte_id=2)
              cteconsumer(cte_id="1_0")
            cteanchor(cte_id=3)
              cteproducer(cte_id=3)
                cteconsumer(cte_id="2_0")
              cteconsumer(cte_id="3_0")
  - sql: |
      with x0 as (select * from t0), x1 as (select * from x0), x2 as (select * from x1), x3 as (select * from x2) select v1 from x3;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1` from `t0` as `t0`")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            cteconsumer(cte_id="0_0")
          cteanchor(cte_id=2)
            cteproducer(cte_id=2)
              cteconsumer(cte_id="1_0")
            cteanchor(cte_id=3)
              cteproducer(cte_id=3)
                cteconsumer(cte_id="2_0")
              cteconsumer(cte_id="3_0")
  - sql: |
      with xx as (select * from t0) select x1.v1 from xx x1 join xx x2 on x1.v2=x2.v3 where x1.v3 = 4 and x2.v2=3;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` where ((`v3` = ?) or (`v2` = ?))")
        project(v1="v1")
          hashjoin(condition="v3 = v2", type="inner")
            cteconsumer(cte_id="0_1", projects=[$2], conditions=[=($1, ?1)])
            cteconsumer(cte_id="0_0", projects=[$0, $1], conditions=[=($2, ?0)])
  - sql: |
      with xx as (select * from t0) select x1.v1 from (select * from xx limit 1) x1 join (select * from xx limit 3) x2 on x1.v2=x2.v3;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        project(v1="v1")
          hashjoin(condition="v3 = v2", type="inner")
            limit(offset=0, fetch=?1)
              cteconsumer(cte_id="0_1", projects=[$2])
            limit(offset=0, fetch=?0)
              cteconsumer(cte_id="0_0", projects=[$0, $1])
  - sql: |
      with xx as (select * from t0) select x1.v1 from (select * from xx where xx.v2 = 2 limit 1) x1 join (select * from xx where xx.v3 = 4 limit 3) x2 on x1.v2=x2.v3;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` where ((`v2` = ?) or (`v3` = ?))")
        project(v1="v1")
          hashjoin(condition="v3 = v2", type="inner")
            filter(condition="v2 = ?2")
              limit(offset=0, fetch=?1)
                cteconsumer(cte_id="0_0", projects=[$0, $1], conditions=[=($1, ?0)])
            filter(condition="v3 = ?0")
              limit(offset=0, fetch=?3)
                cteconsumer(cte_id="0_1", projects=[$2], conditions=[=($2, ?2)])
  - sql: |
      WITH   s AS (select * from t0),   a AS (select * from s),   a2 AS (select * from s),   b AS (    select v3, v1, v2 from s    UNION    select v3 + 1, v1 + 2, v2 + 3 from s  )   select * from b;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            hashagg(group="v3,v1,v2")
              unionall(concurrent=true)
                cteconsumer(cte_id="0_0", projects=[$2, $0, $1])
                cteconsumer(cte_id="0_1", projects=[+($2, ?0), +($0, ?1), +($1, ?2)])
          cteconsumer(cte_id="1_0")
  - sql: |
      WITH   s AS (select * from t0),   a AS (select * from s),   a2 AS (select * from a),   b AS (    select v3, v1, v2 from s    UNION    select v3 + 1, v1 + 2, v2 + 3 from s  )   select * from b;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        cteanchor(cte_id=2)
          cteproducer(cte_id=2)
            hashagg(group="v3,v1,v2")
              unionall(concurrent=true)
                cteconsumer(cte_id="0_1", projects=[$2, $0, $1])
                cteconsumer(cte_id="0_2", projects=[+($2, ?0), +($0, ?1), +($1, ?2)])
          cteconsumer(cte_id="2_0")
  - sql: |
      WITH x1 AS (select * from t0),  x2 AS (select * from x1)  select *  from (select x2.* from x1 join x2 on x1.v2 = x2.v2) as s1 join (select x1.* from x1 join x2 on x1.v3 = x2.v3) as s2 on s1.v2 = s2.v2;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            cteconsumer(cte_id="0_0")
          sortmergejoin(condition="v2 = v20", type="inner")
            project(v1="v1", v20="v2", v3="v3")
              hashjoin(condition="v2 = v2", type="inner")
                memsort(sort="v2 asc")
                  cteconsumer(cte_id="1_0", projects=[$0, $1, $2])
                cteconsumer(cte_id="0_1", projects=[$1])
            project(v1="v1", v2="v2", v3="v3")
              hashjoin(condition="v3 = v3", type="inner")
                memsort(sort="v2 asc")
                  cteconsumer(cte_id="0_3", projects=[$0, $1, $2])
                cteconsumer(cte_id="1_1", projects=[$2])
  - sql: |
      select * from (with xx as (select * from t0) select x1.* from xx x1 join xx x2 on x1.v2 = x2.v2) s where s.v1 = 2;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        project(v1="v1", v2="v20", v3="v3")
          hashjoin(condition="v2 = v2", type="inner")
            cteconsumer(cte_id="0_1", projects=[$1])
            cteconsumer(cte_id="0_0", conditions=[=($0, ?0)])
  - sql: |
      select * from (with xx as (select * from t0) select x1.* from xx x1 left outer join xx x2 on x1.v2 = x2.v2) s where s.v1 = 2 limit 10;
    plan: |
      limit(offset=0, fetch=?1)
        cteanchor(cte_id=0)
          cteproducer(cte_id=0)
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
          project(v1="v1", v2="v20", v3="v3")
            hashjoin(condition="v2 = v2", type="right", build="right")
              cteconsumer(cte_id="0_1", projects=[$1])
              cteconsumer(cte_id="0_0", projects=[$0, $1, $2], conditions=[=($0, ?0)])
  - sql: |
      select * from (with xx as (select * from t0) select x1.* from xx x1 left outer join xx x2 on x1.v2 = x2.v2) s limit 10;
    plan: |
      limit(offset=0, fetch=?0)
        cteanchor(cte_id=0)
          cteproducer(cte_id=0)
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
          project(v1="v1", v2="v20", v3="v3")
            hashjoin(condition="v2 = v2", type="right")
              cteconsumer(cte_id="0_1", projects=[$1])
              cteconsumer(cte_id="0_0", projects=[$0, $1, $2])
  - sql: |
      WITH     w_t0 as (SELECT * FROM t0) SELECT * FROM t1 LEFT JOIN w_t0 ON t1.v4 = w_t0.v1 AND false;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` where false")
        hashjoin(condition="v4 = v1", type="left")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          cteconsumer(cte_id="0_0", conditions=[false])
  - sql: |
      WITH     w_t0 as (SELECT * FROM t0) SELECT * FROM w_t0 LEFT JOIN t1 ON t1.v4 = w_t0.v1 AND true;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        hashjoin(condition="v4 = v1 and true", type="left", build="left")
          cteconsumer(cte_id="0_0")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
  - sql: |
      WITH w_t0 as (SELECT * FROM t0) SELECT x0.v1, x1.v2 FROM  w_t0 x0, w_t0 x1
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0`")
        project(v1="v1", v2="v2")
          nljoin(condition="true", type="inner")
            cteconsumer(cte_id="0_1", projects=[$1])
            cteconsumer(cte_id="0_0", projects=[$0])
  - sql: |
      WITH w_t0 as (SELECT * FROM t0) SELECT v1, v2, v3 FROM  w_t0 x0 where false union select v1, v2, v3 from w_t0 x1 where abs(1) = 2
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` where (abs(?) = ?)")
        hashagg(group="v1,v2,v3")
          unionall(concurrent=true)
            cteconsumer(cte_id="0_0", conditions=[false])
            cteconsumer(cte_id="0_1", conditions=[=(abs(?0), ?1)])
  - sql: |
      WITH w_t0 as (SELECT * FROM t0),           w_t1 as (select * from t1) SELECT v1, v2, v3 FROM w_t0 x0 where false union select v1, v2, v3 from w_t0 x1 where abs(1) = 2 union select v4, v5, v6 from w_t1 x2 where 1 > 2 union select v4, v5, v6 from w_t1 x2 where not null
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` where (abs(?) = ?)")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1` where ((1 > 2) or (not null))")
          hashagg(group="v1,v2,v3")
            unionall(concurrent=true)
              cteconsumer(cte_id="0_0", conditions=[false])
              cteconsumer(cte_id="0_1", conditions=[=(abs(?0), ?1)])
              cteconsumer(cte_id="1_0", conditions=[>(1, 2)])
              cteconsumer(cte_id="1_1", conditions=[not(null)])
  - sql: |
      WITH x1 as (   WITH x2 as (SELECT * FROM t0)   SELECT * from x2   UNION ALL   SELECT * from x2 ) SELECT * from x1 UNION ALL SELECT * from x1
    plan: |
      cteanchor(cte_id=1)
        cteproducer(cte_id=1)
          cteanchor(cte_id=0)
            cteproducer(cte_id=0)
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            unionall(concurrent=true)
              cteconsumer(cte_id="0_0")
              cteconsumer(cte_id="0_1")
        unionall(concurrent=true)
          cteconsumer(cte_id="1_0")
          cteconsumer(cte_id="1_1")
  - sql: |
      WITH x1 as (   WITH x2 as (       WITH x3 as (SELECT * FROM t0)       SELECT * FROM x3       UNION ALL       SELECT * FROM x3   )   SELECT * from x2   UNION ALL   SELECT * from x2 ) SELECT * from x1 UNION ALL SELECT * from x1
    plan: |
      cteanchor(cte_id=4)
        cteproducer(cte_id=4)
          cteanchor(cte_id=1)
            cteproducer(cte_id=1)
              cteanchor(cte_id=0)
                cteproducer(cte_id=0)
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
                unionall(concurrent=true)
                  cteconsumer(cte_id="0_0")
                  cteconsumer(cte_id="0_1")
            unionall(concurrent=true)
              cteconsumer(cte_id="1_0")
              cteconsumer(cte_id="1_1")
        unionall(concurrent=true)
          cteconsumer(cte_id="4_0")
          cteconsumer(cte_id="4_1")
  - sql: |
      WITH x1 as (   WITH x2 as (SELECT * FROM t0)   SELECT * from x2   UNION ALL   SELECT * from x2 ) SELECT * from (   with x3 as (       SELECT * from x1       UNION ALL       SELECT * from x1   )   select * from x3   union all   select * from x3) x4
    plan: |
      cteanchor(cte_id=1)
        cteproducer(cte_id=1)
          cteanchor(cte_id=0)
            cteproducer(cte_id=0)
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            unionall(concurrent=true)
              cteconsumer(cte_id="0_0")
              cteconsumer(cte_id="0_1")
        cteanchor(cte_id=4)
          cteproducer(cte_id=4)
            unionall(concurrent=true)
              cteconsumer(cte_id="1_0")
              cteconsumer(cte_id="1_1")
          unionall(concurrent=true)
            cteconsumer(cte_id="4_0")
            cteconsumer(cte_id="4_1")
  - sql: |
      WITH x1 as ( WITH x2 as ( SELECT * FROM t0 ) SELECT * from x2 UNION ALL SELECT * from x2 ), x5 as ( SELECT * from x1 UNION ALL SELECT * from x1 ) SELECT * from ( with x3 as ( SELECT * from x1 UNION ALL SELECT * from x1 ) select * from x3 union all select * from x3 ) x4 join ( select * from x5 union all select * from x5 ) x7
    plan: |
      cteanchor(cte_id=1)
        cteproducer(cte_id=1)
          cteanchor(cte_id=0)
            cteproducer(cte_id=0)
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            unionall(concurrent=true)
              cteconsumer(cte_id="0_0")
              cteconsumer(cte_id="0_1")
        cteanchor(cte_id=4)
          cteproducer(cte_id=4)
            unionall(concurrent=true)
              cteconsumer(cte_id="1_0")
              cteconsumer(cte_id="1_1")
          project(v1="v10", v2="v20", v3="v30", v10="v1", v20="v2", v30="v3")
            nljoin(condition="true", type="inner")
              unionall(concurrent=true)
                cteconsumer(cte_id="4_0")
                cteconsumer(cte_id="4_1")
              cteanchor(cte_id=7)
                cteproducer(cte_id=7)
                  unionall(concurrent=true)
                    cteconsumer(cte_id="1_2")
                    cteconsumer(cte_id="1_3")
                unionall(concurrent=true)
                  cteconsumer(cte_id="7_0")
                  cteconsumer(cte_id="7_1")
  - sql: |
      WITH x1 as ( select * from t0),  x2 as ( select * from x1 union all select * from x1) SELECT * from x1 UNION ALL SELECT * from x2
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            unionall(concurrent=true)
              cteconsumer(cte_id="0_0")
              cteconsumer(cte_id="0_1")
          unionall(concurrent=true)
            cteconsumer(cte_id="0_2")
            cteconsumer(cte_id="1_0")
  - sql: |
      with x1 as (select * from t0),     x2 as (select * from t0),     x3 as (select * from t0),     x4 as (select * from t0),     x5 as (select * from t0) select * from x1 union all select * from x1 union all select * from x2 union all select * from x2 union all select * from x3 union all select * from x3 union all select * from x4 union all select * from x4 union all select * from x5 union all select * from x5;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
          cteanchor(cte_id=2)
            cteproducer(cte_id=2)
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            cteanchor(cte_id=3)
              cteproducer(cte_id=3)
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
              cteanchor(cte_id=4)
                cteproducer(cte_id=4)
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
                unionall(concurrent=true)
                  cteconsumer(cte_id="0_0")
                  cteconsumer(cte_id="0_1")
                  cteconsumer(cte_id="1_0")
                  cteconsumer(cte_id="1_1")
                  cteconsumer(cte_id="2_0")
                  cteconsumer(cte_id="2_1")
                  cteconsumer(cte_id="3_0")
                  cteconsumer(cte_id="3_1")
                  cteconsumer(cte_id="4_0")
                  cteconsumer(cte_id="4_1")
  - sql: |
      WITH x1 as ( select * from t0) SELECT t1.* from t1, x1 UNION ALL SELECT t0.* from t0, x1
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1` from `t0` as `t0`")
        unionall(concurrent=true)
          project(v4="v4", v5="v5", v6="v6")
            nljoin(condition="true", type="inner")
              gather(concurrent=true)
                logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
              cteconsumer(cte_id="0_0")
          project(v1="v1", v2="v2", v3="v3")
            nljoin(condition="true", type="inner")
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
              cteconsumer(cte_id="0_1")
  - sql: |
      select * from t0 where (t0.v1 in (with c1 as (select v4 as v2 from t1) select x1.v2 from c1 x1 join c1 x2 join c1 x3)) is null;
    plan: |
      project(v1="v1", v2="v2", v3="v3")
        filter(condition="is null(apply_boolean)")
          correlateapply(cor=$cor0, leftconditions=[$0], opkind=equals, type=semi)
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            cteanchor(cte_id=0)
              cteproducer(cte_id=0)
                gather(concurrent=true)
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4` as `v2` from `t1` as `t1`")
              project(v2="v2")
                nljoin(condition="true", type="inner")
                  project(v2="v20")
                    nljoin(condition="true", type="inner")
                      cteconsumer(cte_id="0_1")
                      cteconsumer(cte_id="0_0")
                  cteconsumer(cte_id="0_2", projects=[$0])
      cache node:
      
      gather(concurrent=true)
        logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4` as `v2` from `t1` as `t1`")
        project(v2="v2")
          nljoin(condition="true", type="inner")
            project(v2="v20")
              nljoin(condition="true", type="inner")
                cteconsumer(cte_id="0_1")
                cteconsumer(cte_id="0_0")
            cteconsumer(cte_id="0_2", projects=[$0])
  - sql: |
      WITH with_t_0 as (  SELECT v3 FROM t0 ) SELECT  subt0.v3,  ROW_NUMBER() OVER (PARTITION BY subt0.v3, subt0.v2),  LAST_VALUE(subt0.v1) OVER (ORDER BY subt0.v3) FROM t0 subt0, with_t_0 UNION ALL SELECT  subt0.v3,  ROW_NUMBER() OVER (PARTITION BY subt0.v3, subt0.v2),  LAST_VALUE(subt0.v1) OVER (ORDER BY subt0.v3) FROM t0 subt0, with_t_0
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v3` from `t0` as `t0`")
        unionall(concurrent=true)
          project(v3="v3", $1="f4w0$o0", $2="f5w1$o0")
            sortwindow(v1="v1", v3="v3", f4w0$o0="f4w0$o0", f5w1$o0="window#0last_value($0)", reference windows="window#0=window(partition {} order by [1 asc-nulls-first] range between unbounded preceding and current row aggs [last_value($0)])")
              memsort(sort="v3 asc")
                project(v1="v1", v3="v3", f4w0$o0="f4w0$o0")
                  sortwindow(v1="v1", v2="v2", v3="v3", f4w0$o0="window#0row_number()", reference windows="window#0=window(partition {1, 2} order by [] rows between unbounded preceding and current row aggs [row_number()])")
                    project(v1="v1", v2="v2", v3="v3")
                      nljoin(condition="true", type="inner")
                        mergesort(sort="v2 asc,v3 asc")
                          logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` order by `v2`, `v3`")
                        cteconsumer(cte_id="0_0")
          project(v3="v3", $1="f4w0$o0", $2="f5w1$o0")
            sortwindow(v1="v1", v3="v3", f4w0$o0="f4w0$o0", f5w1$o0="window#0last_value($0)", reference windows="window#0=window(partition {} order by [1 asc-nulls-first] range between unbounded preceding and current row aggs [last_value($0)])")
              memsort(sort="v3 asc")
                project(v1="v1", v3="v3", f4w0$o0="f4w0$o0")
                  sortwindow(v1="v1", v2="v2", v3="v3", f4w0$o0="window#0row_number()", reference windows="window#0=window(partition {1, 2} order by [] rows between unbounded preceding and current row aggs [row_number()])")
                    project(v1="v1", v2="v2", v3="v3")
                      nljoin(condition="true", type="inner")
                        mergesort(sort="v2 asc,v3 asc")
                          logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` order by `v2`, `v3`")
                        cteconsumer(cte_id="0_1")
  - sql: |
      WITH cte_1 AS (  SELECT null v1 from t1 ) SELECT    CASE     WHEN a.v1 = b.v1 THEN 1     ELSE -1   END IS_OK FROM cte_1 a, cte_1 b
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select null as `v1` from `t1` as `t1`")
        project(is_ok="case(=($1, $0), ?0, ?1)")
          nljoin(condition="true", type="inner")
            cteconsumer(cte_id="0_1")
            cteconsumer(cte_id="0_0")
  - sql: |
      WITH cte_1 AS (  SELECT null v1 ) SELECT    CASE     WHEN a.v1 = b.v1 THEN 1     ELSE -1   END IS_OK FROM cte_1 a, cte_1 b
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          project(v1="null")
            values(table="dual")
        project(is_ok="case(=($1, $0), ?0, ?1)")
          nljoin(condition="true", type="inner")
            cteconsumer(cte_id="0_1")
            cteconsumer(cte_id="0_0")
  - sql: |
      with with_t_0 as ( select v1, v2, v4 from t0 join t1 ), with_t_1 as ( select v1, v2, v5 from t0 join t1 ) select v5, 1 from with_t_1 join with_t_0 where with_t_0.v4 < 100 and with_t_0.v1 > 0 and exists ( select 1 from with_t_0 x where v4 = 123 and x.v2 = with_t_0.v1 );
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          nljoin(condition="(v4 < ?1 and v1 > ?2) or (v4 = ?4 and v2 > ?2)", type="inner")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0` where ((`v1` > ?) or (`v2` > ?))")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4` from `t1` as `t1` where ((`v4` < ?) or (`v4` = ?))")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            project(v5="v5")
              nljoin(condition="true", type="inner")
                gather(concurrent=true)
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5` from `t1` as `t1`")
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1` from `t0` as `t0`")
          project(v5="v5", 1="?0")
            nljoin(condition="true", type="inner")
              cteconsumer(cte_id="1_0")
              semihashjoin(condition="v2 = v1", type="semi", build="inner")
                cteconsumer(cte_id="0_0", projects=[$0], conditions=[and(<($2, ?1), >($0, ?2))])
                cteconsumer(cte_id="0_1", projects=[$1], conditions=[=($2, ?4), >($1, ?2)])
  - sql: |
      with x1 as (select * from t0), y1 as (select count(distinct v1, v2), count(distinct v2, v3) from t0), y2 as (select count(distinct v3, v2), count(distinct v1, v3) from t0) select * from y1 join y2         join t1
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          hashagg(count(distinct v1, v2)="count(v1, v2) filter $3", count(distinct v2, v3)="count(v2, v3) filter $4")
            project(v1="v1", v2="v2", v3="v3", $g_1="case(=($3, 1), 1, 4) = 1", $g_4="case(=($3, 1), 1, 4) = 4")
              hashagg(group="v1,v2,v3,$e")
                expand(projects="{v1=v1, v2=v2, v3=null, $e=1}, {v1=null, v2=v2, v3=v3, $e=4}")
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            hashagg(count(distinct v3, v2)="count(v3, v2) filter $3", count(distinct v1, v3)="count(v1, v3) filter $4")
              project(v3="v3", v2="v2", v1="v1", $g_1="case(=($3, 1), 1, 2) = 1", $g_2="case(=($3, 1), 1, 2) = 2")
                hashagg(group="v3,v2,v1,$e")
                  expand(projects="{v3=v3, v2=v2, v1=null, $e=1}, {v3=v3, v2=null, v1=v1, $e=2}")
                    gather(concurrent=true)
                      logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v3`, `v2`, `v1` from `t0` as `t0`")
          project(count(distinct v1, v2)="count(distinct v1, v2)", count(distinct v2, v3)="count(distinct v2, v3)", count(distinct v3, v2)="count(distinct v3, v2)", count(distinct v1, v3)="count(distinct v1, v3)", v4="v4", v5="v5", v6="v6")
            nljoin(condition="true", type="inner")
              gather(concurrent=true)
                logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
              nljoin(condition="true", type="inner")
                cteconsumer(cte_id="0_0")
                cteconsumer(cte_id="1_0")
  - sql: |
      with cte as (select 111) select * from cte join (select * from (select * from t1 join cte) t1) t2
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          project(111="?0")
            values(table="dual")
        project(111="111", v4="v4", v5="v5", v6="v6", 1110="1110")
          nljoin(condition="true", type="inner")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
            nljoin(condition="true", type="inner")
              cteconsumer(cte_id="0_0")
              cteconsumer(cte_id="0_1")
  - sql: |
      select * from (with xx as (select 444 as v2) select x1.* from xx x1 join xx x2 on x1.v2 = x2.v2) s where s.v2 = 444;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          project(v2="?0")
            filter(condition="?0 = ?1")
              values(table="dual")
        project(v2="v20")
          hashjoin(condition="v2 = v2", type="inner")
            cteconsumer(cte_id="0_1", conditions=[=($0, ?1)])
            cteconsumer(cte_id="0_0", conditions=[=($0, ?1)])
  - sql: |
      with cte as (select * from t0) select case when not exists (select 1 from cte where v2 = 1) then 'A' else 'B' end,        case when not exists (select 1 from cte where v3 = 1) then 'C' else 'D' end,         case when not exists (select 1 from cte) then 'E' else 'F' end  from t1;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        project($f0="case($0, ?2, ?3)", $f1="case($1, ?6, ?7)", $f2="case($2, ?9, ?10)")
          correlateapply(cor=$cor0, leftconditions=[], opkind=null, type=anti)
            correlateapply(cor=$cor0, leftconditions=[], opkind=null, type=anti)
              project(apply_boolean="apply_boolean")
                correlateapply(cor=$cor0, leftconditions=[], opkind=null, type=anti)
                  gather(concurrent=true)
                    logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
                  cteconsumer(cte_id="0_0", conditions=[=($1, ?1)])
              cteconsumer(cte_id="0_1", conditions=[=($2, ?5)])
            cteconsumer(cte_id="0_2")
  - sql: |
      with t0 as(select rand() randnum), t1 as(select randnum, 't1_randnum' type from t0), t2 as(select randnum, 't2_randnum' type from t0), t3 as(select randnum, 't3_randnum' type from t0), t4 as(select * from t1 union all select * from t2 union all select * from t3) select * from t4;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          project(randnum="rand()")
            values(table="dual")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            cteconsumer(cte_id="0_0", projects=[$0, ?0])
          cteanchor(cte_id=2)
            cteproducer(cte_id=2)
              cteconsumer(cte_id="0_1", projects=[$0, ?1])
            cteanchor(cte_id=3)
              cteproducer(cte_id=3)
                cteconsumer(cte_id="0_2", projects=[$0, ?2])
              cteanchor(cte_id=4)
                cteproducer(cte_id=4)
                  unionall(concurrent=true)
                    cteconsumer(cte_id="1_0")
                    cteconsumer(cte_id="2_0")
                    cteconsumer(cte_id="3_0")
                cteconsumer(cte_id="4_0")
  - sql: |
      with cte0 as(select * from t0 limit 10), cte1 as(select * from cte0), cte2 as(select * from cte0), cte3 as(select * from cte1 union all select * from cte2) select * from cte3;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          mergesort(sort="", offset=0, fetch=?0)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` limit ?")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            cteconsumer(cte_id="0_0")
          cteanchor(cte_id=2)
            cteproducer(cte_id=2)
              cteconsumer(cte_id="0_1")
            cteanchor(cte_id=3)
              cteproducer(cte_id=3)
                unionall(concurrent=true)
                  cteconsumer(cte_id="1_0")
                  cteconsumer(cte_id="2_0")
              cteconsumer(cte_id="3_0")
  - sql: |
      with cte0 as(select * from t0 order by v1 limit 10), cte1 as(select * from cte0), cte2 as(select * from cte0) select * from cte1 union all select * from cte2;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          mergesort(sort="v1 asc", offset=0, fetch=?0)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) order by `v1` limit ?")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            cteconsumer(cte_id="0_0")
          cteanchor(cte_id=2)
            cteproducer(cte_id=2)
              cteconsumer(cte_id="0_1")
            unionall(concurrent=true)
              cteconsumer(cte_id="1_0")
              cteconsumer(cte_id="2_0")
  - sql: |
      with cte0 as(select rand() as r, v1 from t0 limit 10), cte1 as(select * from cte0), cte2 as(select * from cte0) select * from cte1 union all select * from cte2;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          mergesort(sort="", offset=0, fetch=?0)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select rand() as `r`, `v1` from `t0` as `t0` limit ?")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            cteconsumer(cte_id="0_0")
          cteanchor(cte_id=2)
            cteproducer(cte_id=2)
              cteconsumer(cte_id="0_1")
            unionall(concurrent=true)
              cteconsumer(cte_id="1_0")
              cteconsumer(cte_id="2_0")
  - sql: |
      WITH xx AS (SELECT * FROM t0) SELECT * FROM xx WHERE v1 = 1;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          logicalview(tables="t0[p3]", sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (`v1` = ?)")
        cteconsumer(cte_id="0_0", conditions=[=($0, ?0)])
  - sql: |
      WITH xx AS (SELECT * FROM t0) SELECT * FROM xx WHERE v1 IN (SELECT v4 FROM t1);
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        semihashjoin(condition="v1 = v4", type="semi", build="inner")
          cteconsumer(cte_id="0_0")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4` from `t1` as `t1`")
  - sql: |
      WITH a AS (SELECT * FROM t0), b AS (SELECT * FROM a where v2 < 10 UNION ALL SELECT * FROM a where v1 < 10) SELECT * FROM b WHERE v1 = 1 UNION ALL SELECT * FROM b WHERE v2 = 2;
    plan: |
      cteanchor(cte_id=0)
        cteproducer(cte_id=0)
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` where ((`v2` < ?) or (`v1` < ?) or ((`v2` < ?) and (`v1` = ?)) or ((`v1` < ?) and (`v1` = ?)) or ((`v2` < ?) and (`v2` = ?)) or ((`v1` < ?) and (`v2` = ?)))")
        cteanchor(cte_id=1)
          cteproducer(cte_id=1)
            unionall(concurrent=true)
              cteconsumer(cte_id="0_0", conditions=[<($1, ?0), or(=($0, ?2), =($1, ?3))])
              cteconsumer(cte_id="0_1", conditions=[<($0, ?1), or(=($0, ?2), =($1, ?3))])
          unionall(concurrent=true)
            cteconsumer(cte_id="1_0", conditions=[=($0, ?2)])
            cteconsumer(cte_id="1_1", conditions=[=($1, ?3)])
DDL:
  t0:
    CREATE TABLE `t0` (
    `v1` int(11) NOT NULL,
    `v2` int(11) NOT NULL,
    `v3` int(11) NOT NULL,
    PRIMARY KEY (`v1`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8 partition by hash(`v1`) PARTITIONS 3;

  t1:
    CREATE TABLE `t1` (
    `v4` int(11) NOT NULL,
    `v5` int(11) NOT NULL,
    `v6` int(11) NOT NULL,
    PRIMARY KEY (`v4`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8 partition by hash(`v4`) PARTITIONS 3;

STATISTICS:
  t0:
    168648768
  t1:
    6860984320
CONFIG:
  CTE_MODE:
    REUSE
  defaltxxAPPName.isNew:
    true
