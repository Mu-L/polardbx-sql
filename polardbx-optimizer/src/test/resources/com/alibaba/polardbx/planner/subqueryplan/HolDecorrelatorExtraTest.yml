SQL:
  - sql: |
      select * from t0 left join t1 on  v1 in (select v7 from t2 where v1+v4 = v8)
    plan: |
      err-code: [tddl-4501][err_optimizer] optimize error by holsubqueryremoverule: join sub-query referencing both sides is not supported for jointype=left
  - sql: |
      select * from t0  join t1 on  t0.v1 = (SELECT count(*) FROM t1 WHERE t0.v2 = t1.v5) or v1 in (select v7 from t2 where v1+v4 = v8)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        filter(condition="v1 = $f1 or cast(is not null(v7))")
          project(v1="v10", v2="v2", v3="v3", v20="v20", $f1="$f1", v4="v40", v5="v5", v6="v6", v7="v7", v10="v1", v40="v4")
            hashjoin(condition="v1 = v1 and v4 = v4", type="right")
              project(v7="v7", v1="v1", v4="v4")
                hashjoin(condition="v1 = v7 and v8 = +", type="inner")
                  project(v1="v1", v4="v4", +="v1 + v4")
                    nljoin(condition="true", type="inner")
                      gather(concurrent=true)
                        logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4` from `t1` as `t1`")
                      hashjoin(condition="v2 = v2", type="left")
                        gather(concurrent=true)
                          logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0`")
                        project(v2="v2")
                          hashjoin(condition="v2 <=> v50", type="left", build="left")
                            hashagg(group="v2")
                              gather(concurrent=true)
                                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                            project(v50="v50")
                              hashagg(group="v50", count(*)="sum(count(*))")
                                gather(concurrent=true)
                                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
                  gather(concurrent=true)
                    logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v8` from `t2` as `t2`")
              project(v1="v1", v2="v2", v3="v3", v20="v20", $f1="$f1", v4="v4", v5="v5", v6="v6")
                nljoin(condition="true", type="inner")
                  gather(concurrent=true)
                    logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
                  hashjoin(condition="v2 = v2", type="left")
                    gather(concurrent=true)
                      logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
                    project(v2="v2", $f1="case(is null($2), 0, $2)")
                      hashjoin(condition="v2 <=> v50", type="left", build="left")
                        hashagg(group="v2")
                          gather(concurrent=true)
                            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                        hashagg(group="v50", count(*)="sum(count(*))")
                          gather(concurrent=true)
                            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
  - sql: |
      /*+TDDL:cmd_extra(HOLISTIC_SUBQUERY_UNNEST_DML=false)*/
      INSERT INTO t0 (v1, v2, v3)
      SELECT t0.* FROM t0 JOIN t1 
        ON t0.v1 = (SELECT count(*) FROM t1 WHERE t0.v2 = t1.v5) 
        AND t1.v6 = (SELECT max(v7) FROM t2 WHERE t2.v8 = t0.v1);
    plan: |
      err-code: [tddl-4998][err_not_support] subquery in join not support yet!
  - sql: |
      INSERT INTO t0 (v1, v2, v3)
      SELECT t0.* FROM t0 JOIN t1 
        ON t0.v1 = (SELECT count(*) FROM t1 WHERE t0.v2 = t1.v5) 
        AND t1.v6 = (SELECT max(v7) FROM t2 WHERE t2.v8 = t0.v1);
    plan: |
      logicalinsert(table="t0", columns=recordtype(integer v1, integer v2, integer v3), mode=multi)
        project(v1="v1", v2="v20", v3="v3")
          hashjoin(condition="max(v7) = v6", type="inner")
            hashjoin(condition="v1 = v1", type="left", build="left")
              bkajoin(condition="$f1 = v1 and v2 = v2", type="inner")
                project(v2="v2", $f1="case(is null($2), 0, $2)")
                  hashjoin(condition="v2 <=> v50", type="left", build="left")
                    hashagg(group="v2")
                      gather(concurrent=true)
                        logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                    hashagg(group="v50", count(*)="sum(count(*))")
                      gather(concurrent=true)
                        logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (((`v1`, `v2`)) in (...))")
              project(v1="v1", max(v7)="max(v7)")
                hashjoin(condition="v1 <=> v8", type="left")
                  hashjoin(condition="v2 = v2", type="left")
                    gather(concurrent=true)
                      logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0`")
                    project(v2="v2")
                      hashjoin(condition="v2 <=> v50", type="left", build="left")
                        hashagg(group="v2")
                          gather(concurrent=true)
                            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                        project(v50="v50")
                          hashagg(group="v50", count(*)="sum(count(*))")
                            gather(concurrent=true)
                              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
                  hashagg(group="v8", max(v7)="max(max(v7))")
                    gather(concurrent=true)
                      logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v8`, max(`v7`) as `max(v7)` from `t2` as `t2` where (`v8` = `v8`) group by `v8`")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v6` from `t1` as `t1`")
  - sql: |
      /*+TDDL:cmd_extra(ENABLE_JOIN_SUBQUERY_UNNEST=false)*/
      INSERT INTO t0 (v1, v2, v3)
      SELECT t0.* FROM t0 JOIN t1 
        ON t0.v1 = (SELECT count(*) FROM t1 WHERE t0.v2 = t1.v5) 
        AND t1.v6 = (SELECT max(v7) FROM t2 WHERE t2.v8 = t0.v1);
    plan: |
      err-code: [tddl-4998][err_not_support] subquery in join not support yet!
  - sql: |
      INSERT ignore INTO t0 (v1, v2, v3)
      SELECT t0.* FROM t0 JOIN t1 
        ON t0.v1 = (SELECT count(*) FROM t1 WHERE t0.v2 = t1.v5) 
        AND t1.v6 = (SELECT max(v7) FROM t2 WHERE t2.v8 = t0.v1);
    plan: |
      logicalinsert(table="t0", columns=recordtype(integer v1, integer v2, integer v3), mode=multi)
        project(v1="v1", v2="v20", v3="v3")
          hashjoin(condition="max(v7) = v6", type="inner")
            hashjoin(condition="v1 = v1", type="left", build="left")
              bkajoin(condition="$f1 = v1 and v2 = v2", type="inner")
                project(v2="v2", $f1="case(is null($2), 0, $2)")
                  hashjoin(condition="v2 <=> v50", type="left", build="left")
                    hashagg(group="v2")
                      gather(concurrent=true)
                        logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                    hashagg(group="v50", count(*)="sum(count(*))")
                      gather(concurrent=true)
                        logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (((`v1`, `v2`)) in (...))")
              project(v1="v1", max(v7)="max(v7)")
                hashjoin(condition="v1 <=> v8", type="left")
                  hashjoin(condition="v2 = v2", type="left")
                    gather(concurrent=true)
                      logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0`")
                    project(v2="v2")
                      hashjoin(condition="v2 <=> v50", type="left", build="left")
                        hashagg(group="v2")
                          gather(concurrent=true)
                            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                        project(v50="v50")
                          hashagg(group="v50", count(*)="sum(count(*))")
                            gather(concurrent=true)
                              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
                  hashagg(group="v8", max(v7)="max(max(v7))")
                    gather(concurrent=true)
                      logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v8`, max(`v7`) as `max(v7)` from `t2` as `t2` where (`v8` = `v8`) group by `v8`")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v6` from `t1` as `t1`")
  - sql: |
      REPLACE INTO t0 (v1, v2, v3)
      SELECT t0.* FROM t0 JOIN t1 
        ON t0.v1 = (SELECT count(*) FROM t1 WHERE t0.v2 = t1.v5) 
        AND t1.v6 = (SELECT max(v7) FROM t2 WHERE t2.v8 = t0.v1);
    plan: |
      logicalreplace(table="t0", columns=recordtype(integer v1, integer v2, integer v3), mode=multi)
        project(v1="v1", v2="v20", v3="v3")
          hashjoin(condition="max(v7) = v6", type="inner")
            hashjoin(condition="v1 = v1", type="left", build="left")
              bkajoin(condition="$f1 = v1 and v2 = v2", type="inner")
                project(v2="v2", $f1="case(is null($2), 0, $2)")
                  hashjoin(condition="v2 <=> v50", type="left", build="left")
                    hashagg(group="v2")
                      gather(concurrent=true)
                        logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                    hashagg(group="v50", count(*)="sum(count(*))")
                      gather(concurrent=true)
                        logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (((`v1`, `v2`)) in (...))")
              project(v1="v1", max(v7)="max(v7)")
                hashjoin(condition="v1 <=> v8", type="left")
                  hashjoin(condition="v2 = v2", type="left")
                    gather(concurrent=true)
                      logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0`")
                    project(v2="v2")
                      hashjoin(condition="v2 <=> v50", type="left", build="left")
                        hashagg(group="v2")
                          gather(concurrent=true)
                            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                        project(v50="v50")
                          hashagg(group="v50", count(*)="sum(count(*))")
                            gather(concurrent=true)
                              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
                  hashagg(group="v8", max(v7)="max(max(v7))")
                    gather(concurrent=true)
                      logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v8`, max(`v7`) as `max(v7)` from `t2` as `t2` where (`v8` = `v8`) group by `v8`")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v6` from `t1` as `t1`")
  - sql: |
      INSERT INTO t0 (v1, v2, v3)
      SELECT t0.* FROM t0 JOIN t1 
        ON t0.v1 = (SELECT count(*) FROM t1 WHERE t0.v2 = t1.v5) 
        AND t1.v6 = (SELECT max(v7) FROM t2 WHERE t2.v8 = t0.v1) on duplicate key update v1 = v1;
    plan: |
      logicalupsert(table="t0", columns=recordtype(integer v1, integer v2, integer v3), uniquekeyselect=[select [[v1]] on t0], mode=single)
        project(v1="v1", v2="v20", v3="v3")
          hashjoin(condition="max(v7) = v6", type="inner")
            hashjoin(condition="v1 = v1", type="left", build="left")
              bkajoin(condition="$f1 = v1 and v2 = v2", type="inner")
                project(v2="v2", $f1="case(is null($2), 0, $2)")
                  hashjoin(condition="v2 <=> v50", type="left", build="left")
                    hashagg(group="v2")
                      gather(concurrent=true)
                        logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                    hashagg(group="v50", count(*)="sum(count(*))")
                      gather(concurrent=true)
                        logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (((`v1`, `v2`)) in (...))")
              project(v1="v1", max(v7)="max(v7)")
                hashjoin(condition="v1 <=> v8", type="left")
                  hashjoin(condition="v2 = v2", type="left")
                    gather(concurrent=true)
                      logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0`")
                    project(v2="v2")
                      hashjoin(condition="v2 <=> v50", type="left", build="left")
                        hashagg(group="v2")
                          gather(concurrent=true)
                            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                        project(v50="v50")
                          hashagg(group="v50", count(*)="sum(count(*))")
                            gather(concurrent=true)
                              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
                  hashagg(group="v8", max(v7)="max(max(v7))")
                    gather(concurrent=true)
                      logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v8`, max(`v7`) as `max(v7)` from `t2` as `t2` where (`v8` = `v8`) group by `v8`")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v6` from `t1` as `t1`")
  - sql: |
      /*+TDDL:cmd_extra(ENABLE_JOIN_SUBQUERY_UNNEST=false)*/select * from t0  join t1 on v1 in (select v7 from t2 where v1+v4 = v8) or v1 < v5;
    plan: |
      err-code: [tddl-4998][err_not_support] subquery in join not support yet!
  - sql: |
      select * from t0  join t1 on v1 in (select v7 from t2 where v1+v4 = v8) or v1 < v5;
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        filter(condition="cast(is not null(v7)) or v1 < v5")
          project(v1="v10", v2="v2", v3="v3", v4="v40", v5="v5", v6="v6", v7="v7", v10="v1", v40="v4")
            hashjoin(condition="v1 = v1 and v4 = v4", type="right")
              project(v7="v7", v1="v1", v4="v4")
                hashjoin(condition="v1 = v7 and v8 = +", type="inner")
                  project(v1="v1", v4="v4", +="v1 + v4")
                    nljoin(condition="true", type="inner")
                      gather(concurrent=true)
                        logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4` from `t1` as `t1`")
                      gather(concurrent=true)
                        logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1` from `t0` as `t0`")
                  gather(concurrent=true)
                    logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v8` from `t2` as `t2`")
              project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
                nljoin(condition="true", type="inner")
                  gather(concurrent=true)
                    logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
  - sql: |
      SELECT *
      FROM t0
      WHERE EXISTS (
          SELECT 1
          FROM t1
          JOIN t2
            ON v4 = v7
           AND (
                 SELECT max(v9)
                 FROM t2 AS t2b
                 WHERE t2b.v8 = v1 + v4 + v8     -- L0.v1 + 本层L(t1).v4 + 本层R(t2).v8
               ) > 100
      );
    plan: |
      materializedsemijoin(condition="v1 = v1", type="semi")
        gather(concurrent=true)
          logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (`v1` in (...))")
        project(v1="v11")
          hashjoin(condition="v1 = v1", type="inner")
            project(v7="v7", v8="v8", v9="v9", v4="v40", v5="v5", v6="v6", v40="v400", v1="v10", v10="v1", v41="v4", max(v9)="max(v9)")
              hashjoin(condition="v1 <=> v1 and v40 <=> v4", type="inner")
                filter(condition="max(v9) > ?1")
                  hashagg(group="v1,v4", max(v9)="max(v9)")
                    project(v1="v1", v4="v4", v8="v8", v9="v9")
                      nljoin(condition="v8 = v1 + v4 + v8", type="inner")
                        nljoin(condition="true", type="inner")
                          gather(concurrent=true)
                            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1` from `t0` as `t0`")
                          gather(concurrent=true)
                            logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v8`, `v9` from `t2` as `t2`")
                        gather(concurrent=true)
                          logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4` from `t1` as `t1`")
                project(v7="v7", v8="v8", v9="v9", v4="v4", v5="v5", v6="v6", v40="v40", v1="v1")
                  nljoin(condition="true", type="inner")
                    gather(concurrent=true)
                      logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1` from `t0` as `t0`")
                    project(v7="v7", v8="v8", v9="v9", v4="v40", v5="v5", v6="v6", v40="v4")
                      hashjoin(condition="v4 = v4", type="inner")
                        gather(concurrent=true)
                          logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4` from `t1` as `t1`")
                        project(v7="v7", v8="v8", v9="v9", v4="v4", v5="v5", v6="v6")
                          hashjoin(condition="v7 = v4", type="inner")
                            gather(concurrent=true)
                              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
                            gather(concurrent=true)
                              logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v8`, `v9` from `t2` as `t2`")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1` from `t0` as `t0`")
  - sql: |
      select * from t0 join t1 on t0.v1 = (select count(*) from t1 where t0.v2 = t1.v5)
    plan: |
      project(v1="v1", v2="v20", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          bkajoin(condition="$f1 = v1 and v2 = v2", type="inner")
            project(v2="v2", $f1="case(is null($1), 0, $1)")
              hashjoin(condition="v2 <=> v50", type="right", build="right")
                hashagg(group="v50", count(*)="sum(count(*))")
                  gather(concurrent=true)
                    logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
                hashagg(group="v2")
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (((`v1`, `v2`)) in (...))")
  - sql: |
      select * from t0 join t1 on (select count(*) from t0 where t0.v2 = t1.v5) = t1.v5
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v50", v6="v6")
        nljoin(condition="true", type="inner")
          project(v5="v50", $f1="$f1", v4="v4", v50="v5", v6="v6")
            hashjoin(condition="$f1 = v5 and v5 = v5", type="inner")
              gather(concurrent=true)
                logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
              project(v5="v5", $f1="case(is null($2), 0, $2)")
                filter(condition="case(is null($2), 0, $2) = v5")
                  hashjoin(condition="v5 <=> v20", type="left", build="left")
                    hashagg(group="v5")
                      gather(concurrent=true)
                        logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5` from `t1` as `t1` group by `v5`")
                    hashagg(group="v20", count(*)="sum(count(*))")
                      gather(concurrent=true)
                        logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2`, count(*) as `count(*)` from `t0` as `t0` where (`v2` = `v2`) group by `v2`")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
  - sql: |
      select * from t0 join t1 on t0.v1 = (select count(*) from t1 where t0.v2 = t1.v5) + t1.v6
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="v1 = $f1 + v6", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          hashjoin(condition="v2 = v2", type="inner")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            project(v2="v2", $f1="case(is null($1), 0, $1)")
              hashjoin(condition="v2 <=> v50", type="right", build="right")
                hashagg(group="v50", count(*)="sum(count(*))")
                  gather(concurrent=true)
                    logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
                hashagg(group="v2")
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
  - sql: |
      select * from t0 join t1 on t0.v1 = t1.v4 and t0.v3 = (select count(*) from t1 where t0.v2 = t1.v5)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        hashjoin(condition="$f1 = v3 and v2 = v2", type="inner")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3],t1[p1,p2,p3]", shardcount=3, sql="select `t0`.`v1`, `t0`.`v2`, `t0`.`v3`, `t1`.`v4`, `t1`.`v5`, `t1`.`v6` from `t0` as `t0` inner join `t1` as `t1` on (`t0`.`v1` = `t1`.`v4`)")
          project(v2="v2", $f1="case(is null($1), 0, $1)")
            hashjoin(condition="v2 <=> v50", type="right", build="right")
              hashagg(group="v50", count(*)="sum(count(*))")
                gather(concurrent=true)
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
              hashagg(group="v2")
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
  - sql: |
      select * from t0 join t1 on (select count(*) from t0 where t0.v2 = t1.v5) + t0.v3 = t1.v5
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="v5 = $f1 + v3", type="inner")
          hashjoin(condition="v5 = v5", type="inner")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
            project(v5="v5", $f1="case(is null($1), 0, $1)")
              hashjoin(condition="v5 <=> v20", type="right", build="right")
                hashagg(group="v20", count(*)="sum(count(*))")
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2`, count(*) as `count(*)` from `t0` as `t0` where (`v2` = `v2`) group by `v2`")
                hashagg(group="v5")
                  gather(concurrent=true)
                    logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5` from `t1` as `t1` group by `v5`")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
  - sql: |
      select * from t0 join t1 on t0.v1 = (select count(*) from t1 where t0.v2 = t1.v5 and t0.v3 = t1.v6)
    plan: |
      project(v1="v1", v2="v20", v3="v30", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          bkajoin(condition="v3 = v3 and $f2 = v1 and v2 = v2", type="inner")
            project(v2="v2", v3="v3", $f2="case(is null($2), 0, $2)")
              hashjoin(condition="v2 <=> v50 and v3 <=> v60", type="right", build="right")
                hashagg(group="v50,v60", count(*)="sum(count(*))")
                  gather(concurrent=true)
                    logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, `v6`, count(*) as `count(*)` from `t1` as `t1` where ((`v5` = `v5`) and (`v6` = `v6`)) group by `v5`, `v6`")
                hashagg(group="v2,v3")
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2`, `v3` from `t0` as `t0` group by `v2`, `v3`")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` where (((`v3`, `v1`, `v2`)) in (...))")
  - sql: |
      select * from t0 join t1 on t0.v1 = (select count(*) from t1 where t0.v2 = t1.v5) and t1.v6 = (select max(v7) from t2 where t2.v8 = t0.v1)
    plan: |
      project(v1="v1", v2="v20", v3="v3", v4="v4", v5="v5", v6="v6")
        hashjoin(condition="max(v7) = v6", type="inner")
          hashjoin(condition="v1 = v1", type="left", build="left")
            bkajoin(condition="$f1 = v1 and v2 = v2", type="inner")
              project(v2="v2", $f1="case(is null($2), 0, $2)")
                hashjoin(condition="v2 <=> v50", type="left", build="left")
                  hashagg(group="v2")
                    gather(concurrent=true)
                      logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                  hashagg(group="v50", count(*)="sum(count(*))")
                    gather(concurrent=true)
                      logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (((`v1`, `v2`)) in (...))")
            project(v1="v1", max(v7)="max(v7)")
              hashjoin(condition="v1 <=> v8", type="left")
                hashjoin(condition="v2 = v2", type="left")
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0`")
                  project(v2="v2")
                    hashjoin(condition="v2 <=> v50", type="left", build="left")
                      hashagg(group="v2")
                        gather(concurrent=true)
                          logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                      project(v50="v50")
                        hashagg(group="v50", count(*)="sum(count(*))")
                          gather(concurrent=true)
                            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5`, count(*) as `count(*)` from `t1` as `t1` where (`v5` = `v5`) group by `v5`")
                hashagg(group="v8", max(v7)="max(max(v7))")
                  gather(concurrent=true)
                    logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v8`, max(`v7`) as `max(v7)` from `t2` as `t2` where (`v8` = `v8`) group by `v8`")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
  - sql: |
      select * from t0 join t1 on t0.v1 = (select v4 from t1 where t0.v2 = t1.v5)
    plan: |
      project(v1="v1", v2="v20", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          bkajoin(condition="$f1 = v1 and v2 = v2", type="inner")
            hashjoin(condition="v2 <=> v5", type="left", build="left")
              hashagg(group="v2")
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
              hashagg(group="v5", $f1="single_value(v4)")
                gather(concurrent=true)
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5` from `t1` as `t1` where (`v5` = `v5`)")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (((`v1`, `v2`)) in (...))")
  - sql: |
      select * from t0 join t1 on t0.v1 = t1.v4 and t0.v3 = (select v4 from t1 where t0.v2 = t1.v5)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        hashjoin(condition="$f1 = v3 and v2 = v2", type="inner")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3],t1[p1,p2,p3]", shardcount=3, sql="select `t0`.`v1`, `t0`.`v2`, `t0`.`v3`, `t1`.`v4`, `t1`.`v5`, `t1`.`v6` from `t0` as `t0` inner join `t1` as `t1` on (`t0`.`v1` = `t1`.`v4`)")
          hashjoin(condition="v2 <=> v5", type="left", build="left")
            hashagg(group="v2")
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
            hashagg(group="v5", $f1="single_value(v4)")
              gather(concurrent=true)
                logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5` from `t1` as `t1` where (`v5` = `v5`)")
  - sql: |
      select * from t0 join t1 on t0.v1 = (select v4 from t1 where t0.v2 = t1.v5 and t1.v5 = 10 and t1.v6 != 3)
    plan: |
      project(v1="v1", v2="v20", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          bkajoin(condition="$f1 = v1 and v2 = v2", type="inner")
            hashjoin(condition="v2 <=> v50", type="left", build="left")
              hashagg(group="v2")
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
              hashagg(group="v50", $f1="single_value(v4)")
                gather(concurrent=true)
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5` as `v50` from `t1` as `t1` where ((`v5` = ?) and (`v5` = `v5`) and (`v6` <> ?))")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (((`v1`, `v2`)) in (...))")
  - sql: |
      select * from t0 join t1 on (select v1 from t0,t2 where t0.v2 = t1.v5 and t0.v2 = t2.v7) * 2 = t1.v4
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v50", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
          bkajoin(condition="v4 = __*__ and v5 = v5", type="inner")
            hashjoin(condition="v5 <=> v20", type="left", build="left")
              hashagg(group="v5")
                gather(concurrent=true)
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5` from `t1` as `t1` group by `v5`")
              project(v20="v2", __*__="$f1 * ?0")
                hashagg(group="v2", $f1="single_value(v1)")
                  project(v7="v7", v1="v1", v2="v2")
                    hashjoin(condition="v2 = v7", type="inner")
                      gather(concurrent=true)
                        logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0` where (`v2` = `v2`)")
                      gather(concurrent=true)
                        logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7` from `t2` as `t2`")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1` force index(`primary`) where (((`v4`, `v5`)) in (...))")
  - sql: |
      select * from t0 join t1 on t0.v1 = t1.v4 and t0.v3 = (select v4 from t1 where t0.v2 = t1.v5) and t1.v6 = (select v8 from t2 where t2.v9 = t0.v3)
    plan: |
      project(v1="v1", v2="v20", v3="v3", v4="v4", v5="v50", v6="v6")
        hashjoin(condition="$f10 = v6 and v4 = v1", type="inner")
          hashjoin(condition="v3 = v3", type="left")
            hashjoin(condition="$f1 = v3 and v2 = v2", type="inner")
              hashjoin(condition="v2 <=> v5", type="left", build="left")
                hashagg(group="v2")
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                hashagg(group="v5", $f1="single_value(v4)")
                  gather(concurrent=true)
                    logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5` from `t1` as `t1` where (`v5` = `v5`)")
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            project(v3="v3", $f1="$f1")
              hashjoin(condition="v3 <=> v9", type="left", build="left")
                hashagg(group="v3")
                  hashjoin(condition="v2 = v2", type="left")
                    gather(concurrent=true)
                      logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
                    project(v2="v2", $f1="$f1")
                      hashjoin(condition="v2 <=> v5", type="left", build="left")
                        hashagg(group="v2")
                          gather(concurrent=true)
                            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                        hashagg(group="v5", $f1="single_value(v4)")
                          gather(concurrent=true)
                            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5` from `t1` as `t1` where (`v5` = `v5`)")
                hashagg(group="v9", $f1="single_value(v8)")
                  gather(concurrent=true)
                    logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v8`, `v9` from `t2` as `t2` where (`v9` = `v9`)")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
  - sql: |
      select * from t0 join t1 on t0.v1 = (select count(*) from t3 join t2)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          bkajoin(condition="v1 = count(*)", type="inner")
            hashagg(count(*)="count()")
              project(v10="v10", v7="v7")
                nljoin(condition="true", type="inner")
                  gather(concurrent=true)
                    logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7` from `t2` as `t2`")
                  gather(concurrent=true)
                    logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v10` from `t3` as `t3`")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (`v1` in (...))")
  - sql: |
      select * from t0 join t1 on (select count(*) from t3) = t1.v5
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        hashjoin(condition="v5 = count(*)", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          nljoin(condition="true", type="left")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            hashagg(count(*)="sum(count(*))")
              gather(concurrent=true)
                logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select count(*) as `count(*)` from `t3` as `t3`")
  - sql: |
      select * from t0 join t1 on t0.v1 = (select count(*) from t1)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          bkajoin(condition="v1 = count(*)", type="inner")
            hashagg(count(*)="sum(count(*))")
              gather(concurrent=true)
                logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select count(*) as `count(*)` from `t1` as `t1`")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (`v1` in (...))")
  - sql: |
      select * from t0 join t1 on t0.v1 = (select count(*) from t0)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          bkajoin(condition="v1 = count(*)", type="inner")
            hashagg(count(*)="sum(count(*))")
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select count(*) as `count(*)` from `t0` as `t0`")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (`v1` in (...))")
  - sql: |
      select * from t0 join t1 on t0.v1 = t1.v4 and t0.v3 = (select count(*) from t0)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        hashjoin(condition="v3 = count(*)", type="inner")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3],t1[p1,p2,p3]", shardcount=3, sql="select `t0`.`v1`, `t0`.`v2`, `t0`.`v3`, `t1`.`v4`, `t1`.`v5`, `t1`.`v6` from `t0` as `t0` inner join `t1` as `t1` on (`t0`.`v1` = `t1`.`v4`)")
          hashagg(count(*)="sum(count(*))")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select count(*) as `count(*)` from `t0` as `t0`")
  - sql: |
      select * from t0 join t1 on t0.v1 = t1.v4 and t0.v3 = (select count(*) from t0) and t1.v5 != (select count(*) from t2)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        bkajoin(condition="v4 = v1 and v5 <> count(*)0", type="inner")
          nljoin(condition="true", type="left")
            project(count(*)="count(*)", v1="v1", v2="v2", v3="v3")
              hashjoin(condition="v3 = count(*)", type="inner")
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
                hashagg(count(*)="sum(count(*))")
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select count(*) as `count(*)` from `t0` as `t0`")
            hashagg(count(*)="sum(count(*))")
              gather(concurrent=true)
                logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select count(*) as `count(*)` from `t2` as `t2`")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1` force index(`primary`) where (`v4` in (...))")
  - sql: |
      select * from t0 join t1 on t0.v1 = (select t2.v8 from t2 where t2.v7 = 10)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          bkajoin(condition="v1 = $f0", type="inner")
            logicalview(tables="t2[p3]", sql="select (case count(`v8`) when 0 then null when 1 then `v8` else (((select null union all select null))) end) as `$f0` from `t2` as `t2` force index(`primary`) where (`v7` = ?)")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (`v1` in (...))")
  - sql: |
      select * from t0 join t1 on t0.v1 = (select t2.v7 + t3.v10 from t2 join t3)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          bkajoin(condition="v1 = $f0", type="inner")
            hashagg($f0="single_value(t2.v7 + t3.v10)")
              project(t2.v7 + t3.v10="v7 + v10")
                nljoin(condition="true", type="inner")
                  gather(concurrent=true)
                    logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7` from `t2` as `t2`")
                  gather(concurrent=true)
                    logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v10` from `t3` as `t3`")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (`v1` in (...))")
  - sql: |
      select * from t0 join t1 on t0.v1 = t1.v4 and t0.v3 = (select t2.v8 from t2 where t2.v7 = 10)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        hashjoin(condition="v3 = $f0", type="inner")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3],t1[p1,p2,p3]", shardcount=3, sql="select `t0`.`v1`, `t0`.`v2`, `t0`.`v3`, `t1`.`v4`, `t1`.`v5`, `t1`.`v6` from `t0` as `t0` inner join `t1` as `t1` on (`t0`.`v1` = `t1`.`v4`)")
          logicalview(tables="t2[p3]", sql="select (case count(`v8`) when 0 then null when 1 then `v8` else (((select null union all select null))) end) as `$f0` from `t2` as `t2` force index(`primary`) where (`v7` = ?)")
  - sql: |
      select * from t0 join t1 on t0.v1 = t1.v4 and t0.v3 = (select t2.v8 from t2 where t2.v7 = 10) and t0.v2 = (select t2.v9 from t2 where t2.v8 < 100)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        hashjoin(condition="v3 = $f0", type="inner")
          hashjoin(condition="$f0 = v2", type="inner")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3],t1[p1,p2,p3]", shardcount=3, sql="select `t0`.`v1`, `t0`.`v2`, `t0`.`v3`, `t1`.`v4`, `t1`.`v5`, `t1`.`v6` from `t0` as `t0` inner join `t1` as `t1` on (`t0`.`v1` = `t1`.`v4`)")
            hashagg($f0="single_value(v9)")
              gather(concurrent=true)
                logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v9` from `t2` as `t2` where (`v8` < ?)")
          logicalview(tables="t2[p3]", sql="select (case count(`v8`) when 0 then null when 1 then `v8` else (((select null union all select null))) end) as `$f0` from `t2` as `t2` force index(`primary`) where (`v7` = ?)")
  - sql: |
      select * from t0 join t1 on t0.v1 = t1.v4 and t0.v1 = (exists (select v7 from t2 where t2.v8 = 1))
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        hashjoin(condition="v4 = is not null and v4 = v1", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          project(v1="v1", v2="v2", v3="v3", is not null="is not null(i)")
            filter(condition="v1 = is not null(i)")
              nljoin(condition="true", type="left")
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
                hashagg(group="i")
                  gather(concurrent=true)
                    logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select true as `i` from `t2` as `t2` where (`v8` = ?) group by true")
  - sql: |
      select * from t0 join t1 on t0.v1 = (exists (select v7 from t2 where t2.v8 = 1))
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          filter(condition="v1 = is not null(i)")
            nljoin(condition="true", type="left")
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
              hashagg(group="i")
                gather(concurrent=true)
                  logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select true as `i` from `t2` as `t2` where (`v8` = ?) group by true")
  - sql: |
      select * from t0 join t1 on t0.v1 = (not exists (select v7 from t2 where t2.v8 = 1)) and t1.v4 = (exists(select v10 from t3 where t3.v11 < 5))
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        hashjoin(condition="v4 = is not null", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          project(v1="v1", v2="v2", v3="v3", is not null="is not null(i)")
            nljoin(condition="true", type="right")
              hashagg(group="i")
                gather(concurrent=true)
                  logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select true as `i` from `t3` as `t3` where (`v11` < ?) group by true")
              filter(condition="v1 = is null(i)")
                nljoin(condition="true", type="left")
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
                  hashagg(group="i")
                    gather(concurrent=true)
                      logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select true as `i` from `t2` as `t2` where (`v8` = ?) group by true")
  - sql: |
      select * from t0 join t1 on t0.v1 = t1.v4 and t0.v1 = (exists (select v7 from t2 where t2.v8 = t1.v5))
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        hashjoin(condition="v1 = is not null and v1 = v4", type="inner")
          project(v4="v4", v5="v5", v6="v6", is not null="is not null(i)")
            filter(condition="v4 = is not null(i)")
              hashjoin(condition="v5 = v80", type="left")
                gather(concurrent=true)
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
                hashagg(group="i,v80")
                  gather(concurrent=true)
                    logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select true as `i`, `v8` as `v80` from `t2` as `t2` where (`v8` = `v8`) group by true, `v8`")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
  - sql: |
      select * from t0 join t1 on t0.v1 = (exists (select v7 from t2 where t2.v8 = t1.v5))
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        hashjoin(condition="v1 = is not null", type="inner")
          project(v4="v4", v5="v5", v6="v6", is not null="is not null(i)")
            hashjoin(condition="v5 = v80", type="left")
              gather(concurrent=true)
                logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
              hashagg(group="i,v80")
                gather(concurrent=true)
                  logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select true as `i`, `v8` as `v80` from `t2` as `t2` where (`v8` = `v8`) group by true, `v8`")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
  - sql: |
      select * from t0 join t1 on t0.v1 = (exists (select v7 from t2 where t2.v8 = t1.v5)) and t0.v2 = (not exists(select v11 from t3 where t3.v10 = t0.v1))
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        hashjoin(condition="v1 = is not null", type="inner")
          project(v4="v4", v5="v5", v6="v6", is not null="is not null(i)")
            hashjoin(condition="v5 = v80", type="left")
              gather(concurrent=true)
                logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
              hashagg(group="i,v80")
                gather(concurrent=true)
                  logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select true as `i`, `v8` as `v80` from `t2` as `t2` where (`v8` = `v8`) group by true, `v8`")
          filter(condition="v2 = is null(i)")
            hashjoin(condition="v1 = v100", type="left")
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
              gather(concurrent=true)
                logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select true as `i`, `v10` as `v100` from `t3` as `t3` where (`v10` = `v10`) group by true, `v10`")
  - sql: |
      select * from t0 join t1 on t0.v1 = t1.v4 and t0.v1 in (select v7 from t2 where t2.v8 = 1)
    plan: |
      bkajoin(condition="v1 = v4", type="inner")
        semihashjoin(condition="v1 = v7", type="semi", build="inner")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
          gather(concurrent=true)
            logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7` from `t2` as `t2` where (`v8` = ?)")
        gather(concurrent=true)
          logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1` force index(`primary`) where (`v4` in (...))")
  - sql: |
      select * from t0 join t1 on t0.v1 in (select v7 from t2)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          semihashjoin(condition="v1 = v7", type="semi", build="inner")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            gather(concurrent=true)
              logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7` from `t2` as `t2`")
  - sql: |
      select * from t0 join t1 on t0.v1 not in (select v7 from t2) and t0.v2 in (select v8 from t2)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          semihashjoin(condition="v2 = v8", type="semi", build="inner")
            filter(condition="cast(case(or(is null($4), =($4, 0)), true, =($5, 1), false, =($6, 1), null, true))")
              project(v1="v10", v2="v2", v3="v3", v10="v1", $f1="$f1", strict="strict", possible="possible")
                hashjoin(condition="v1 = v1", type="right", build="right")
                  project(v1="v10", $f1="case(is null($1), 0, $1)", strict="strict", possible="possible")
                    hashjoin(condition="v1 <=> v1", type="right")
                      sortagg(group="v1", c="count()", strict="max(s_case)", possible="max(p_case)")
                        project(s_case="case(is true(=($0, $1)), 1, 0)", p_case="case(or(=($0, $1), $2), 1, 0)", v1="v1")
                          nljoin(condition="true", type="inner")
                            mergesort(sort="v1 asc")
                              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1` from `t0` as `t0` force index(`primary`) order by `v1`")
                            gather(concurrent=true)
                              logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, (`v7` is null) as `is null` from `t2` as `t2`")
                      gather(concurrent=true)
                        logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1` from `t0` as `t0`")
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            gather(concurrent=true)
              logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v8` from `t2` as `t2`")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
  - sql: |
      select * from t0 join t1 on t0.v1 = t1.v4 and t0.v1 in (select v7 from t2 where t2.v9 = t0.v3)
    plan: |
      bkajoin(condition="v1 = v4", type="inner")
        materializedsemijoin(condition="v1 = v70 and v3 = v90", type="semi")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (((`v1`, `v3`)) in (...))")
          gather(concurrent=true)
            logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7` as `v70`, `v9` as `v90` from `t2` as `t2` where ((`v9` = `v9`) and (`v7` = `v7`))")
        gather(concurrent=true)
          logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1` force index(`primary`) where (`v4` in (...))")
  - sql: |
      select * from t0 join t1 on t0.v1 in (select v7 from t2 where t2.v9 = t0.v3)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          materializedsemijoin(condition="v1 = v70 and v3 = v90", type="semi")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (((`v1`, `v3`)) in (...))")
            gather(concurrent=true)
              logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7` as `v70`, `v9` as `v90` from `t2` as `t2` where ((`v9` = `v9`) and (`v7` = `v7`))")
  - sql: |
      select * from t0 join t1 on t0.v1 in (select v7 from t2 where t2.v9 = t0.v3) and t0.v2 not in (select v8 from t2 where t2.v9 = t0.v2)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          filter(condition="cast(case(or(is null($4), =($4, 0)), true, =($5, 1), false, =($6, 1), null, true))")
            hashjoin(condition="v2 = v2", type="left", build="left")
              materializedsemijoin(condition="v1 = v70 and v3 = v90", type="semi")
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (((`v1`, `v3`)) in (...))")
                gather(concurrent=true)
                  logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7` as `v70`, `v9` as `v90` from `t2` as `t2` where ((`v9` = `v9`) and (`v7` = `v7`))")
              project(v2="v2", $f1="case(is null($2), 0, $2)", strict="strict", possible="possible")
                hashjoin(condition="v2 <=> v90", type="left", build="left")
                  hashagg(group="v2")
                    materializedsemijoin(condition="v1 = v70 and v3 = v90", type="semi")
                      gather(concurrent=true)
                        logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (((`v1`, `v3`)) in (...))")
                      gather(concurrent=true)
                        logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7` as `v70`, `v9` as `v90` from `t2` as `t2` where ((`v9` = `v9`) and (`v7` = `v7`))")
                  hashagg(group="v90", c="sum(c)", strict="max(strict)", possible="max(possible)")
                    gather(concurrent=true)
                      logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v9` as `v90`, count(*) as `c`, max((case when ((`v9` = `v8`) is true) then 1 else 0 end)) as `strict`, max((case when ((`v9` = `v8`) or (`v8` is null)) then 1 else 0 end)) as `possible` from `t2` as `t2` where (`v9` = `v9`) group by `v9`")
  - sql: |
      select * from t0 join t3 on 1 < 1 and v2 = (select v4 from t1);
    plan: |
      project(v1="v1", v2="v2", v3="v3", v10="v10", v11="v11", v12="v12")
        nljoin(condition="true", type="inner")
          project($f0="$f0", v1="v1", v2="v2", v3="v3")
            hashjoin(condition="v2 = $f0", type="inner")
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
              hashagg($f0="single_value(v4)")
                gather(concurrent=true)
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4` from `t1` as `t1`")
          gather(concurrent=true)
            logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v10`, `v11`, `v12` from `t3` as `t3` where (1 < 1)")
  - sql: |
      select * from t0 join t3 on 1 = 1 or v2 = (select v4 from t1);
    plan: |
      project(v1="v1", v2="v2", v3="v3", v10="v10", v11="v11", v12="v12")
        nljoin(condition="true", type="inner")
          filter(condition="1 = 1 or v2 = $f0")
            nljoin(condition="true", type="left")
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
              hashagg($f0="single_value(v4)")
                gather(concurrent=true)
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4` from `t1` as `t1`")
          gather(concurrent=true)
            logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v10`, `v11`, `v12` from `t3` as `t3`")
  - sql: |
      select * from t0 join t3 on v1 = 'a' or (1 = 2 and v2 = (select v4 from t1));
    plan: |
      project(v1="v1", v2="v2", v3="v3", v10="v10", v11="v11", v12="v12")
        nljoin(condition="true", type="inner")
          filter(condition="v1 = ?0 or (1 = 2 and v2 = $f0)")
            nljoin(condition="true", type="left")
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
              hashagg($f0="single_value(v4)")
                gather(concurrent=true)
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4` from `t1` as `t1`")
          gather(concurrent=true)
            logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v10`, `v11`, `v12` from `t3` as `t3`")
  - sql: |
      select * from t0 join t3 on v1 = 'a' and (1 < 2 or v2 = (select v4 from t1));
    plan: |
      project(v1="v1", v2="v2", v3="v3", v10="v10", v11="v11", v12="v12")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v10`, `v11`, `v12` from `t3` as `t3`")
          filter(condition="1 < 2 or v2 = $f0")
            nljoin(condition="true", type="left")
              logicalview(tables="t0[p1]", sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (`v1` = ?)")
              hashagg($f0="single_value(v4)")
                gather(concurrent=true)
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4` from `t1` as `t1`")
  - sql: |
      select * from t0  join t1 on v1 in (select v7 from t2 where v4 = v8) or v1 < v5;
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        filter(condition="cast(is not null(v7)) or v1 < v5")
          project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6", v7="v7", v70="v70", v80="v80")
            hashjoin(condition="v1 = v70 and v4 = v80", type="right")
              gather(concurrent=true)
                logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v7` as `v70`, `v8` as `v80` from `t2` as `t2` where ((`v8` = `v8`) and (`v7` = `v7`))")
              project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
                nljoin(condition="true", type="inner")
                  gather(concurrent=true)
                    logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
  - sql: |
      select * from t0 left join t1 on exists (select v7 from t2 where v1 = v7) or v1 < v5;
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="is not null(i) or v1 < v5", type="right")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          hashjoin(condition="v1 = v70", type="left")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            gather(concurrent=true)
              logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select true as `i`, `v7` as `v70` from `t2` as `t2` where (`v7` = `v7`)")
  - sql: |
      select * from t0 join t1 on exists (select 1 from t2 where t2.v7 = t0.v1 and t2.v8 = t0.v2)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          semihashjoin(condition="v1 <=> v70 and v2 <=> v80", type="semi", build="inner")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            gather(concurrent=true)
              logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v8`, `v9`, `v7` as `v70`, `v8` as `v80` from `t2` as `t2` where ((`v7` = `v7`) and (`v8` = `v8`))")
  - sql: |
      select * from t0 join t1 on exists (select 1 from t2 where t2.v7 = t1.v4 and t2.v8 = t1.v5)
    plan: |
      nljoin(condition="true", type="inner")
        gather(concurrent=true)
          logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        semihashjoin(condition="v4 <=> v70 and v5 <=> v80", type="semi", build="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          gather(concurrent=true)
            logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v8`, `v9`, `v7` as `v70`, `v8` as `v80` from `t2` as `t2` where ((`v7` = `v7`) and (`v8` = `v8`))")
  - sql: |
      select * from t0 join t1 on exists (select 1 from t2 where t2.v7 = t0.v1 and t2.v8 = t1.v5)
    plan: |
      semihashjoin(condition="v1 <=> v70 and v5 <=> v80", type="semi", build="inner")
        project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
          nljoin(condition="true", type="inner")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        gather(concurrent=true)
          logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v8`, `v9`, `v7` as `v70`, `v8` as `v80` from `t2` as `t2` where ((`v7` = `v7`) and (`v8` = `v8`))")
  - sql: |
      select * from t0 join t1 on t0.v1 = t1.v4 and exists (select 1 from t2 where t2.v7 = t0.v2 and t2.v8 = t1.v5)
    plan: |
      semihashjoin(condition="v2 <=> v70 and v5 <=> v80", type="semi", build="inner")
        gather(concurrent=true)
          logicalview(tables="t0[p1,p2,p3],t1[p1,p2,p3]", shardcount=3, sql="select `t0`.`v1`, `t0`.`v2`, `t0`.`v3`, `t1`.`v4`, `t1`.`v5`, `t1`.`v6` from `t0` as `t0` inner join `t1` as `t1` on (`t0`.`v1` = `t1`.`v4`)")
        gather(concurrent=true)
          logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v8`, `v9`, `v7` as `v70`, `v8` as `v80` from `t2` as `t2` where ((`v7` = `v7`) and (`v8` = `v8`))")
  - sql: |
      select * from t0 join t1 on t1.v4 in (select v7 from t2 where t2.v8 = t1.v5)
    plan: |
      nljoin(condition="true", type="inner")
        gather(concurrent=true)
          logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        materializedsemijoin(condition="v4 = v70 and v5 = v80", type="semi")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1` force index(`primary`) where (((`v4`, `v5`)) in (...))")
          gather(concurrent=true)
            logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7` as `v70`, `v8` as `v80` from `t2` as `t2` where ((`v8` = `v8`) and (`v7` = `v7`))")
  - sql: |
      select * from t0 join t1 on t0.v1 in (select v7 from t2 where t2.v8 = t0.v2 and t2.v9 = t1.v5)
    plan: |
      semihashjoin(condition="v1 = v70 and v2 <=> v80 and v5 <=> v90", type="semi", build="inner")
        project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
          nljoin(condition="true", type="inner")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
        gather(concurrent=true)
          logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v7` as `v70`, `v8` as `v80`, `v9` as `v90` from `t2` as `t2` where ((`v8` = `v8`) and (`v9` = `v9`) and (`v7` = `v7`))")
  - sql: |
      select * from t0 join t1 on t0.v1 = t1.v4 and t1.v5 in (select v8 from t2 where t2.v9 = t0.v3)
    plan: |
      semihashjoin(condition="v3 = v90 and v5 = v80", type="semi", build="inner")
        gather(concurrent=true)
          logicalview(tables="t0[p1,p2,p3],t1[p1,p2,p3]", shardcount=3, sql="select `t0`.`v1`, `t0`.`v2`, `t0`.`v3`, `t1`.`v4`, `t1`.`v5`, `t1`.`v6` from `t0` as `t0` inner join `t1` as `t1` on (`t0`.`v1` = `t1`.`v4`)")
        gather(concurrent=true)
          logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v8`, `v9` as `v90`, `v8` as `v80` from `t2` as `t2` where ((`v9` = `v9`) and (`v8` = `v8`))")
  - sql: |
      select * from t0 join t1 on t0.v1 in (select v7 from t2 where t2.v8 = t1.v5) and t1.v4 in (select v10 from t3 where t3.v11 = t0.v2)
    plan: |
      semihashjoin(condition="v2 = v110 and v4 = v100", type="semi", build="inner")
        semihashjoin(condition="v1 = v70 and v5 = v80", type="semi", build="inner")
          project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
            nljoin(condition="true", type="inner")
              gather(concurrent=true)
                logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
          gather(concurrent=true)
            logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v7` as `v70`, `v8` as `v80` from `t2` as `t2` where ((`v8` = `v8`) and (`v7` = `v7`))")
        gather(concurrent=true)
          logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v10`, `v11` as `v110`, `v10` as `v100` from `t3` as `t3` where ((`v11` = `v11`) and (`v10` = `v10`))")
  - sql: |
      select * from t0 join t1 on t1.v4 = (select max(v7) from t2 where t2.v8 = t1.v5)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v50", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
          bkajoin(condition="max(v7) = v4 and v5 = v5", type="inner")
            hashjoin(condition="v5 <=> v8", type="left", build="left")
              hashagg(group="v5")
                gather(concurrent=true)
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5` from `t1` as `t1` group by `v5`")
              hashagg(group="v8", max(v7)="max(max(v7))")
                gather(concurrent=true)
                  logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v8`, max(`v7`) as `max(v7)` from `t2` as `t2` where (`v8` = `v8`) group by `v8`")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1` force index(`primary`) where (((`v4`, `v5`)) in (...))")
  - sql: |
      select * from t0 join t1 on t0.v1 = (select max(v7) from t2 where t2.v8 = t0.v2 and t2.v9 = t1.v5)
    plan: |
      project(v1="v1", v2="v20", v3="v3", v4="v4", v5="v5", v6="v6")
        hashjoin(condition="v5 = v5", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          bkajoin(condition="v2 = v2 and max(v7) = v1", type="inner")
            project(v2="v2", v5="v5", v80="v8", v90="v9", max(v7)="max(v7)")
              nljoin(condition="v2 <=> v8 and v5 <=> v9", type="right")
                hashagg(group="v8,v9", max(v7)="max(max(v7))")
                  gather(concurrent=true)
                    logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v8`, `v9`, max(`v7`) as `max(v7)` from `t2` as `t2` where ((`v8` = `v8`) and (`v9` = `v9`)) group by `v8`, `v9`")
                hashagg(group="v2,v5")
                  project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
                    nljoin(condition="true", type="inner")
                      gather(concurrent=true)
                        logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
                      gather(concurrent=true)
                        logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` where (((`v2`, `v1`)) in (...))")
  - sql: |
      select * from t0 join t1 on t0.v1 = t1.v4 and t0.v2 > (select min(v7) from t2 where t2.v8 = t0.v3 and t2.v9 = t1.v6)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        hashjoin(condition="v6 = v6 and v3 = v3 and v2 > min(v7)", type="inner")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3],t1[p1,p2,p3]", shardcount=3, sql="select `t0`.`v1`, `t0`.`v2`, `t0`.`v3`, `t1`.`v4`, `t1`.`v5`, `t1`.`v6` from `t0` as `t0` inner join `t1` as `t1` on (`t0`.`v1` = `t1`.`v4`)")
          hashjoin(condition="v3 <=> v8 and v6 <=> v9", type="left", build="left")
            hashagg(group="v3,v6")
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3],t1[p1,p2,p3]", shardcount=3, sql="select `t0`.`v3`, `t1`.`v6` from `t0` as `t0` inner join `t1` as `t1` on (`t0`.`v1` = `t1`.`v4`) group by `t0`.`v3`, `t1`.`v6`")
            hashagg(group="v8,v9", min(v7)="min(min(v7))")
              gather(concurrent=true)
                logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v8`, `v9`, min(`v7`) as `min(v7)` from `t2` as `t2` where ((`v8` = `v8`) and (`v9` = `v9`)) group by `v8`, `v9`")
  - sql: |
      select * from t0 join t1 on t0.v1 = (select max(v7) from t2 where t2.v8 = t0.v2) and t1.v4 = (select min(v10) from t3 where t3.v11 = t1.v5)
    plan: |
      project(v1="v1", v2="v20", v3="v3", v4="v4", v5="v50", v6="v6")
        nljoin(condition="true", type="inner")
          bkajoin(condition="max(v7) = v1 and v2 = v2", type="inner")
            hashjoin(condition="v2 <=> v8", type="left", build="left")
              hashagg(group="v2")
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
              hashagg(group="v8", max(v7)="max(max(v7))")
                gather(concurrent=true)
                  logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v8`, max(`v7`) as `max(v7)` from `t2` as `t2` where (`v8` = `v8`) group by `v8`")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (((`v1`, `v2`)) in (...))")
          bkajoin(condition="min(v10) = v4 and v5 = v5", type="inner")
            hashjoin(condition="v5 <=> v11", type="left", build="left")
              hashagg(group="v5")
                gather(concurrent=true)
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5` from `t1` as `t1` group by `v5`")
              hashagg(group="v11", min(v10)="min(min(v10))")
                gather(concurrent=true)
                  logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v11`, min(`v10`) as `min(v10)` from `t3` as `t3` where (`v11` = `v11`) group by `v11`")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1` force index(`primary`) where (((`v4`, `v5`)) in (...))")
  - sql: |
      select * from t0 join t1 on exists (select 1 from t2 where t2.v7 = t0.v1 and t2.v8 = t0.v2 and t2.v9 = t0.v3)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          semihashjoin(condition="v1 <=> v70 and v2 <=> v80 and v3 <=> v90", type="semi", build="inner")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            gather(concurrent=true)
              logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v8`, `v9`, `v7` as `v70`, `v8` as `v80`, `v9` as `v90` from `t2` as `t2` where ((`v7` = `v7`) and (`v8` = `v8`) and (`v9` = `v9`))")
  - sql: |
      select * from t0 join t1 on (select count(*) from t2 where t2.v7 = t1.v4 and t2.v8 = t1.v5 and t2.v9 = t1.v6) > 0
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          hashjoin(condition="v5 = v5 and v6 = v6 and v4 = v4", type="inner")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
            filter(condition="case(is null($6), 0, $6) > ?0")
              hashjoin(condition="v4 <=> v7 and v5 <=> v80 and v6 <=> v90", type="left")
                gather(concurrent=true)
                  logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
                gather(concurrent=true)
                  logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, (`v8`) as `v80`, (`v9`) as `v90`, count(*) as `count(*)` from `t2` as `t2` where ((`v7` = `v7`) and (`v8` = `v8`) and (`v9` = `v9`)) group by `v7`")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
  - sql: |
      select * from t0 join t1 on t0.v1 in (select v7 from t2 where t2.v8 = t0.v2 and t2.v9 = t0.v3)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          semihashjoin(condition="v1 = v70 and v2 <=> v80 and v3 <=> v90", type="semi", build="inner")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            gather(concurrent=true)
              logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v7` as `v70`, `v8` as `v80`, `v9` as `v90` from `t2` as `t2` where ((`v8` = `v8`) and (`v9` = `v9`) and (`v7` = `v7`))")
  - sql: |
      select * from t0 join t1 on t0.v1 = (select max(v7) from t2 where t2.v7 = t0.v1 and t2.v8 = t0.v2 and t2.v9 = t0.v3)
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          gather(concurrent=true)
            logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
          project(v1="v1", v2="v2", v3="v3", v10="v10", v20="v20", v30="v30", v70="v70", v80="v80", v90="v90", max(v7)="max(v7)")
            bkajoin(condition="v2 = v2 and v3 = v3 and v1 = v1 and v1 <=> v70 and v2 <=> v80 and v3 <=> v90 and max(v7) = v1", type="inner")
              project(v1="v1", v2="v2", v3="v3", v70="v7", v80="v80", v90="v90", max(v7)="max(v7)")
                bkajoin(condition="max(v7) = v1", type="inner")
                  gather(concurrent=true)
                    logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, (`v80`) as `v80`, (`v90`) as `v90`, max(`max(v7)`) as `max(v7)` from (select `v7`, (`v8`) as `v80`, (`v9`) as `v90`, max(`v7`) as `max(v7)` from `t2` as `t2` where ((`v7` = `v7`) and (`v8` = `v8`) and (`v9` = `v9`)) group by `v7`) as `t0` group by `v7`")
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (`v1` in (...))")
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` where (((`v2`, `v3`, `v1`)) in (...))")
  - sql: |
      select * from t0 join t1
        on t0.v1 = (select max(v7) from t2 where t2.v8 = (select min(v10) from t3 where t3.v11 = t0.v2))
        and t1.v4 = (select count(*) from t2 where t2.v9 = (select max(v12) from t3 where t3.v10 = t1.v5))
    plan: |
      project(v1="v1", v2="v21", v3="v3", v4="v4", v5="v50", v6="v6")
        bkajoin(condition="max(v7) = v1 and v2 = v2", type="inner")
          nljoin(condition="true", type="inner")
            bkajoin(condition="$f1 = v4 and v5 = v5", type="inner")
              project(v5="v5", $f1="case(is null($2), 0, $2)")
                hashjoin(condition="v5 <=> v5", type="left", build="left")
                  hashagg(group="v5")
                    gather(concurrent=true)
                      logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5` from `t1` as `t1` group by `v5`")
                  hashagg(group="v5", count(*)="count()")
                    project(v5="v5", v100="v100", max(v12)="max(v12)", v9="v9")
                      hashjoin(condition="v9 = max(v12)", type="inner")
                        gather(concurrent=true)
                          logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v9` from `t2` as `t2`")
                        hashjoin(condition="v5 <=> v100", type="left", build="left")
                          hashagg(group="v5")
                            gather(concurrent=true)
                              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v5` from `t1` as `t1` group by `v5`")
                          gather(concurrent=true)
                            logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v10`, max(`v12`) as `max(v12)` from `t3` as `t3` where (`v10` = `v10`) group by `v10`")
              gather(concurrent=true)
                logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1` force index(`primary`) where (((`v4`, `v5`)) in (...))")
            hashjoin(condition="v2 <=> v2", type="left", build="left")
              hashagg(group="v2")
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
              hashagg(group="v2", max(v7)="max(v7)")
                project(v2="v2", v110="v11", min(v10)="min(v10)", v7="v7", v8="v8")
                  hashjoin(condition="min(v10) = v8", type="inner")
                    gather(concurrent=true)
                      logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v8` from `t2` as `t2`")
                    hashjoin(condition="v2 <=> v11", type="left", build="left")
                      hashagg(group="v2")
                        gather(concurrent=true)
                          logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                      hashagg(group="v11", min(v10)="min(min(v10))")
                        gather(concurrent=true)
                          logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v11`, min(`v10`) as `min(v10)` from `t3` as `t3` where (`v11` = `v11`) group by `v11`")
          gather(concurrent=true)
            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0` force index(`primary`) where (((`v1`, `v2`)) in (...))")
  - sql: |
      select * from t0 join t1
        on t0.v1 in (select v7 from t2 where t2.v8 in (select v10 from t3 where t3.v11 = t0.v2))
        and t1.v4 in (select v7 from t2 where t2.v9 in (select v10 from t3 where t3.v11 = t1.v5))
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          semihashjoin(condition="v4 = v4 and v5 = v5", type="semi", build="inner")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
            project(v7="v7", v4="v4", v5="v5")
              semihashjoin(condition="v4 <=> v4 and v5 <=> v5 and v9 = v10", type="semi", build="inner")
                project(v7="v7", v9="v9", v4="v4", v5="v5")
                  hashjoin(condition="v4 = v7", type="inner")
                    gather(concurrent=true)
                      logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5` from `t1` as `t1`")
                    gather(concurrent=true)
                      logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v9` from `t2` as `t2`")
                project(v10="v10", v4="v4", v5="v5")
                  hashjoin(condition="v11 = v5", type="inner")
                    gather(concurrent=true)
                      logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5` from `t1` as `t1`")
                    gather(concurrent=true)
                      logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v10`, `v11` from `t3` as `t3`")
          semihashjoin(condition="v1 = v1 and v2 = v2", type="semi", build="inner")
            gather(concurrent=true)
              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
            project(v7="v7", v1="v1", v2="v2")
              semihashjoin(condition="v1 <=> v1 and v2 <=> v2 and v8 = v10", type="semi", build="inner")
                project(v7="v7", v8="v8", v1="v1", v2="v2")
                  hashjoin(condition="v1 = v7", type="inner")
                    gather(concurrent=true)
                      logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0`")
                    gather(concurrent=true)
                      logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v8` from `t2` as `t2`")
                project(v10="v10", v1="v1", v2="v2")
                  hashjoin(condition="v11 = v2", type="inner")
                    gather(concurrent=true)
                      logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0`")
                    gather(concurrent=true)
                      logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v10`, `v11` from `t3` as `t3`")
  - sql: |
      select * from t0 join t1
        on t0.v1 = (select max(v7) from t2 where t2.v8 in (select v10 from t3 where t3.v11 = t0.v2))
        and t1.v4 in (select v7 from t2 where t2.v9 = (select max(v10) from t3 where t3.v11 = t1.v5))
        and t0.v3 > (select count(*) from t3 where t3.v12 = (select min(v9) from t2 where t2.v7 = t0.v1))
    plan: |
      project(v1="v1", v2="v2", v3="v3", v4="v4", v5="v5", v6="v6")
        nljoin(condition="true", type="inner")
          hashjoin(condition="v1 = v1 and v1 = max(v7) and v3 > case(is null($10), 0, $10)", type="inner")
            hashjoin(condition="v2 = v2 and v1 = max(v7)", type="inner")
              gather(concurrent=true)
                logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2`, `v3` from `t0` as `t0`")
              hashjoin(condition="v2 <=> v2", type="left", build="left")
                hashagg(group="v2")
                  gather(concurrent=true)
                    logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                sortagg(group="v2", max(v7)="max(v7)")
                  semihashjoin(condition="v2 <=> v11 and v8 = v10", type="semi", build="inner")
                    nljoin(condition="true", type="inner")
                      memsort(sort="v2 asc")
                        hashagg(group="v2")
                          gather(concurrent=true)
                            logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                      gather(concurrent=true)
                        logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v8` from `t2` as `t2`")
                    gather(concurrent=true)
                      logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v10`, `v11` from `t3` as `t3` where (`v11` = `v11`)")
            hashjoin(condition="v1 <=> v1", type="left")
              hashjoin(condition="v2 = v2", type="left")
                gather(concurrent=true)
                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0`")
                project(v2="v2")
                  hashjoin(condition="v2 <=> v2", type="left", build="left")
                    hashagg(group="v2")
                      gather(concurrent=true)
                        logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                    project(v2="v2")
                      sortagg(group="v2", max(v7)="max(v7)")
                        semihashjoin(condition="v2 <=> v11 and v8 = v10", type="semi", build="inner")
                          nljoin(condition="true", type="inner")
                            memsort(sort="v2 asc")
                              hashagg(group="v2")
                                gather(concurrent=true)
                                  logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                            gather(concurrent=true)
                              logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v8` from `t2` as `t2`")
                          gather(concurrent=true)
                            logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v10`, `v11` from `t3` as `t3` where (`v11` = `v11`)")
              hashagg(group="v1", count(*)="count()")
                hashjoin(condition="min(v9) = v12", type="inner")
                  gather(concurrent=true)
                    logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v12` from `t3` as `t3`")
                  hashjoin(condition="v1 <=> v7", type="left")
                    hashjoin(condition="v2 = v2", type="left")
                      gather(concurrent=true)
                        logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v1`, `v2` from `t0` as `t0`")
                      project(v2="v2")
                        hashjoin(condition="v2 <=> v2", type="left", build="left")
                          hashagg(group="v2")
                            gather(concurrent=true)
                              logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                          project(v2="v2")
                            sortagg(group="v2", max(v7)="max(v7)")
                              semihashjoin(condition="v2 <=> v11 and v8 = v10", type="semi", build="inner")
                                nljoin(condition="true", type="inner")
                                  memsort(sort="v2 asc")
                                    hashagg(group="v2")
                                      gather(concurrent=true)
                                        logicalview(tables="t0[p1,p2,p3]", shardcount=3, sql="select `v2` from `t0` as `t0` group by `v2`")
                                  gather(concurrent=true)
                                    logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v8` from `t2` as `t2`")
                                gather(concurrent=true)
                                  logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v10`, `v11` from `t3` as `t3` where (`v11` = `v11`)")
                    gather(concurrent=true)
                      logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, min(`v9`) as `min(v9)` from `t2` as `t2` where (`v7` = `v7`) group by `v7`")
          semihashjoin(condition="v4 = v4 and v5 = v5", type="semi", build="inner")
            gather(concurrent=true)
              logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5`, `v6` from `t1` as `t1`")
            project(v4="v4", v5="v5")
              hashjoin(condition="max(v10) = v9 and v7 = v4", type="inner")
                gather(concurrent=true)
                  logicalview(tables="t2[p1,p2,p3]", shardcount=3, sql="select `v7`, `v9` from `t2` as `t2`")
                hashjoin(condition="v4 <=> v4 and v5 <=> v5", type="left")
                  gather(concurrent=true)
                    logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5` from `t1` as `t1`")
                  hashagg(group="v4", v5="__first_value(v5)", max(v10)="max(max(v10))")
                    bkajoin(condition="v11 = v5", type="inner")
                      hashagg(group="v11", max(v10)="max(max(v10))")
                        gather(concurrent=true)
                          logicalview(tables="t3[p1,p2,p3]", shardcount=3, sql="select `v11`, max(`v10`) as `max(v10)` from `t3` as `t3` group by `v11`")
                      gather(concurrent=true)
                        logicalview(tables="t1[p1,p2,p3]", shardcount=3, sql="select `v4`, `v5` from `t1` as `t1` where (`v5` in (...))")
DDL:
  t0:
    CREATE TABLE `t0` (
    `v1` int(11) NOT NULL,
    `v2` int(11) NOT NULL,
    `v3` int(11) NOT NULL,
    PRIMARY KEY (`v1`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8 partition by key(`v1`) PARTITIONS 3;
  t1:
    CREATE TABLE `t1` (
    `v4` int(11) NOT NULL,
    `v5` int(11) NOT NULL,
    `v6` int(11) NOT NULL,
    PRIMARY KEY (`v4`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8 partition by key(`v4`) PARTITIONS 3;
  t2:
    CREATE TABLE `t2` (
    `v7` bigint NULL COMMENT "",
    `v8` bigint NULL COMMENT "",
    `v9` bigint NULL,
    PRIMARY KEY (`v7`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8 partition by key(`v7`) PARTITIONS 3;
  t3:
    CREATE TABLE `t3` (
    `v10` bigint NULL COMMENT "",
    `v11` bigint NULL COMMENT "",
    `v12` bigint NULL
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8 partition by key(`v10`) PARTITIONS 3;
  test_all_type:
    CREATE TABLE `test_all_type` (
    `t1a` varchar(20) NULL COMMENT "",
    `t1b` smallint(6) NULL COMMENT "",
    `t1c` int(11) NULL COMMENT "",
    `t1d` bigint(20) NULL COMMENT "",
    `t1e` float NULL COMMENT "",
    `t1f` double NULL COMMENT "",
    `t1g` bigint(20) NULL COMMENT "",
    `id_datetime` datetime NULL COMMENT "",
    `id_date` date NULL COMMENT "",
    `id_decimal` decimal(9, 3) NULL COMMENT ""
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8 partition by key(`t1a`) PARTITIONS 3;
  test_all_type_not_null:
    CREATE TABLE `test_all_type_not_null` (
    `t1a` varchar(20) NOT NULL COMMENT "",
    `t1b` smallint(6) NOT NULL COMMENT "",
    `t1c` int(11) NOT NULL COMMENT "",
    `t1d` bigint(20) NOT NULL COMMENT "",
    `t1e` float NOT NULL COMMENT "",
    `t1f` double NOT NULL COMMENT "",
    `t1g` bigint(20) NOT NULL COMMENT "",
    `id_datetime` datetime NOT NULL COMMENT "",
    `id_date` date NOT NULL COMMENT "",
    `id_decimal` decimal(9, 3) NOT NULL COMMENT ""
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8 partition by key(`t1a`) PARTITIONS 3;
STATISTICS:
  t0:
    168648768
  t1:
    6860984320
  t2:
    28609843
  t3:
    3860320
  test_all_type:
    984320
  test_all_type_not_null:
    3168648768
CONFIG:
  FORCE_HOLISTIC_SUBQUERY_UNNEST:
    false
  ENABLE_JOIN_SUBQUERY_UNNEST:
    true
  defaltxxAPPName.isNew:
    true
