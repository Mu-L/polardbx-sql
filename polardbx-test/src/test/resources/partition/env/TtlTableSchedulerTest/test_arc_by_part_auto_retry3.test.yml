## DISABLE_FAST_SQL_PARSER
## RESERVE_SEMICOLONS_FLAG

set TTL_DEBUG_USE_GSI_FOR_COLUMNAR_ARC_TBL = false;
set TTL_DEBUG_CCI_SKIP_DDL_TASKS = "WaitColumnarTableAlterPartitionTask,WaitColumnarTableCreationTask";
set TTL_DEBUG_CURRENT_DATETIME='2024-06-27 00:00:00';
set SKIP_DDL_TASKS="WaitColumnarTableAlterPartitionTask,WaitColumnarTableCreationTask";
set TTL_FORBID_DROP_TTL_TBL_WITH_ARC_CCI = false;

use polardbx;
drop database if exists test_arcpart_retry_db3;
create database if not exists test_arcpart_retry_db3 mode='auto';
use test_arcpart_retry_db3;

drop table if exists ttl_rng_t1;
create table ttl_rng_t1(
a int not null auto_increment,
b datetime,
primary key(a)
)
partition by range columns(`b`)
(
partition p0 values less than ('1970-01-01'),
partition p1 values less than ('1970-02-01'),
partition p2 values less than ('1970-03-01')
);
alter table ttl_rng_t1
modify ttl
ttl_enable = 'on',
ttl_cleanup = 'on',
ttl_expr = `b` expire after 1 day timezone '+08:00',
ttl_job = cron '0 0 1 1 1 ? */1' timezone '+08:00',
ttl_part_interval=interval(1,day),
archive_type='partition',
archive_table_pre_allocate=2,
archive_table_post_allocate=4
;
show create table ttl_rng_t1;


drop table if exists ttl_rng_t2;
create table ttl_rng_t2(
a int not null auto_increment,
b datetime,
primary key(a)
)
partition by range columns(`b`)
(
partition p0 values less than ('1970-01-01'),
partition p1 values less than ('1970-02-01'),
partition p2 values less than ('1970-03-01')
);
alter table ttl_rng_t2
modify ttl
ttl_enable = 'on',
ttl_cleanup = 'on',
ttl_expr = `b` expire after 1 day timezone '+08:00',
ttl_job = cron '0 0 1 1 1 ? */1' timezone '+08:00',
ttl_part_interval=interval(1,day),
archive_type='partition',
archive_table_pre_allocate=2,
archive_table_post_allocate=4
;
show create table ttl_rng_t2;



/*+TDDL:cmd_extra(TTL_JOB_MAINTENANCE_ENABLE=true,TTL_DEBUG_CURRENT_DATETIME='2024-06-27 02:00:00',TTL_JOB_MAINTENANCE_TIME_START='00:00:01',TTL_JOB_MAINTENANCE_TIME_END='23:59:59',DDL_TASK_ERROR_RETRY_WAIT_TIME=1,TTL_ALTER_ADD_PART_STMT_EXTRA_PARAMS='FP_FAIL_ON_SET_TABLE_GROUP_CHANGE_META=true,FP_SUSPEND_ON_SET_TABLE_GROUP_CHANGE_META=3000')*/fire schedule by table ttl_rng_t1;

select sleep(30);
select table_schema,table_name,schedule_name,job_type,lastjob_state from information_schema.schedule_jobs where table_name='ttl_rng_t1' and table_schema='test_arcpart_retry_db3' and lastjob_remark like '%auto rollback%' order by schedule_id;

/*+TDDL:cmd_extra(TTL_JOB_MAINTENANCE_ENABLE=true,TTL_DEBUG_CURRENT_DATETIME='2024-06-27 02:00:00',TTL_JOB_MAINTENANCE_TIME_START='00:00:01',TTL_JOB_MAINTENANCE_TIME_END='23:59:59',TTL_WAIT_TIME_BEFORE_EACH_DDL_STMT_RETRY=5000,TTL_MAX_RETRY_TIME_FOR_PAUSED_CLEANUP_DDL_JOB=2,DDL_TASK_ERROR_RETRY_WAIT_TIME=1,TTL_ALTER_ADD_PART_STMT_EXTRA_PARAMS='FP_FAILED_TABLES_SYNC=true')*/fire schedule by table ttl_rng_t1;

select sleep(60);
select table_schema,table_name,schedule_name,job_type,lastjob_state from information_schema.schedule_jobs where table_name='ttl_rng_t1' and table_schema='test_arcpart_retry_db3' and lastjob_remark like '%the paused ddl%' order by schedule_id;

use polardbx;
drop database if exists test_arcpart_retry_db3;

